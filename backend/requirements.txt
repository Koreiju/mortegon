# web_fiber_haptics backend dependencies — pinned to the verified working
# set (2026-06-14, HYG-01). The four no-mocks subsystems (gpt4all SLM +
# nomic embedder, selenium, langgraph) are pinned exactly; previously
# langgraph/selenium/webdriver-manager were imported but UNLISTED, and
# kuzu was pinned at a stale 0.3.2.

# --- Web framework ---
fastapi==0.127.0
uvicorn==0.40.0
pydantic==2.12.5

# --- Graph DB (file-based; kuzu>=0.11 nests as kuzu_db/data.kuzu; was 0.3.2) ---
kuzu==0.11.3

# --- SLM + embedder (Nous-Hermes-2-Mistral-7B-DPO + nomic-embed-text, CUDA) ---
gpt4all==2.8.2
einops>=0.7.0

# --- Compute graph (no fake gate — required for the compile chain) ---
langgraph==1.1.9

# --- Web scanner (Selenium + Firefox via backend/drivers/geckodriver.exe) ---
selenium==4.39.0
webdriver-manager==4.0.2

# --- Numerics / layout / retrieval ---
numpy==2.1.3
umap-learn==0.5.9.post2
scikit-learn==1.6.1
networkx==3.3
datasketch==1.9.0

# --- Media / IO ---
Pillow>=10.2.0
aiohttp>=3.9.3
