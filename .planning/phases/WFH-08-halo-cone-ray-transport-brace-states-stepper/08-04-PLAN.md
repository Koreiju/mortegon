---
phase: 08-halo-cone-ray-transport-brace-states-stepper
plan: 04
type: execute
wave: 4
depends_on: [08-02, 08-03]
files_modified:
  - scripts/probe_live_cone_transport.py
autonomous: false
requirements: [HALO-03]
tags: [halo, cone-transport, real-subsystem, probe, acceptance, blocking-human]

must_haves:
  truths:
    - "A new probe_live_cone_transport.py asserts, against REAL subsystems (real nomic + TF-IDF over a real scan), that cone placement is monotonic in the real normalized triple-product score (per D-01)"
    - "The probe asserts deleting the top halo result transports the next-most-similar candidate into the vacated cone slot, against real retrieval (per D-01 / §O.18)"
    - "The probe carries a --self-test stub gate so a non-functional probe cannot pass parse-only (per the planning_context D-01 requirement, modeled on Phase 7's 07-06 Task-4 pattern)"
    - "The probe confirms /api/subsystem_status.all_real == True before asserting (no-mocks contract, §8D.46)"
  artifacts:
    - path: "scripts/probe_live_cone_transport.py"
      provides: "the D-01 real-subsystem cone-transport acceptance probe + its --self-test gate"
      min_lines: 60
  key_links:
    - from: "scripts/probe_live_cone_transport.py"
      to: "GET /api/apparitions/{focal_id}?transport=1&ray_project=1"
      via: "real triple-product retrieval after a real scan; asserts transport.{radial,along_ray} monotonic in transport.similarity"
      pattern: "apparitions.*transport"
    - from: "scripts/probe_live_cone_transport.py"
      to: "GET /api/subsystem_status"
      via: "asserts all_real == True before the acceptance assertions (no-mocks)"
      pattern: "subsystem_status|all_real"
---

<objective>
Realize the D-01 acceptance proof for HALO-03: a NEW `scripts/probe_live_cone_transport.py` that
verifies the cone transport against the REAL triple-product retrieval (real nomic + TF-IDF indices
over a real archive.org scan), driven from the MAIN context with a clean-GPU preflight.

This is a BLOCKING-HUMAN, MAIN-CONTEXT-ONLY acceptance task (autonomous: false), modeled on
Phase 7's 07-06 Task-4 checkpoint. It is NOT auto-executed: a wedged CUDA/Selenium boot hangs an
agent, so the real backend lifecycle is driven from the main context per the standing env
discipline (STATE.md: clean GPU ≈0 MiB VRAM / 0 stray python+firefox before the real boot; backend
8080 vs REPL 8000; tear down with `taskkill /F /T`). The stub-backed `halo.spec.js` cone e2e (08-02)
and the `apparition-mode`/`halo-chain-roundtrip` env-scenarios are the FAST gate that runs in both
modes; THIS probe is the real-subsystem acceptance on top.

A `--self-test` stub gate (per the D-01 requirement, mirroring
`scripts/probe_live_duckduckgo_walkthrough.py`'s `run_self_test`) ensures a non-functional probe
cannot pass parse-only: the self-test proves the all_real gate FIRES against a stub backend and
that the monotonicity/transport assertions HAVE TEETH (a deliberately-broken candidate set fails
them).

Purpose: The HALO-03 acceptance bar — real retrieval, real cone placement, real delete-transports-next.
Output: `scripts/probe_live_cone_transport.py` with both the real run and the `--self-test` gate.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-CONTEXT.md
@.planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan (08-04) produces the D-01 real-subsystem cone-transport acceptance probe
(`scripts/probe_live_cone_transport.py`) + its `--self-test` stub gate. It DEPENDS ON the cone
geometry (08-02) and the stepper backend (08-03) being merged. It produces no frontend code — it is
the acceptance proof for the artifacts 08-02 already built.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author probe_live_cone_transport.py — real-retrieval cone assertions + --self-test gate</name>
  <read_first>
    - scripts/probe_live_duckduckgo_walkthrough.py lines 1-49 (the header + run-as banner + the --self-test invocation example), 110-119 (assert_all_real + the unit-testable assertion helpers), 222-234 (the /api/subsystem_status all_real preflight section), 396-465 (run_self_test — the EXACT self-test structure to mirror: (a) the all_real gate fires against a stub backend, (b) the assertion scaffold has teeth)
    - scripts/probe_live_archive_scan.py (the real Selenium scan trigger → poll WS until done → real /api/chunk_search retrieval → pin pattern — the real-scan half to mirror)
    - backend/api/routes.py get_apparitions transport=True branch (the contract the probe calls: GET /api/apparitions/{focal_id}?transport=1&ray_project=1 returns candidates[].transport.{similarity,radial,along_ray})
    - .planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-CONTEXT.md lines 38-48 (D-01: the exact acceptance bar — real scan → open halo → assert cone placement monotonic in the real normalized score + delete-transports-next-in; clean-GPU preflight; main-context-only)
    - .planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-PATTERNS.md lines 228-238 (the probe analog + the exact assertion shape: assert transport.{radial,along_ray} monotonic in transport.similarity across the candidate list → delete top → re-call → assert next-most-similar occupies the vacated slot)
  </read_first>
  <action>
    Author NEW `scripts/probe_live_cone_transport.py` mirroring probe_live_duckduckgo_walkthrough.py's
    structure (the established D-01 model). Real run sequence:
    (1) GET /api/subsystem_status → assert all_real == True (assert_all_real helper, §8D.46) BEFORE
        any acceptance assertion;
    (2) trigger a real archive.org scan (mirror probe_live_archive_scan.py — real Selenium, real
        chunks), poll the WS until `done`;
    (3) pick a focal (a scanned chunk or a query element with a data-3d-node-id) and call
        GET /api/apparitions/{focal_id}?transport=1&ray_project=1 against the REAL triple-product
        index;
    (4) ASSERT monotonicity: across the returned candidates, the cone distance derived from
        transport.{radial,along_ray} is MONOTONIC in transport.similarity (the real normalized
        pagerank·tfidf·nomic score) — more similar → nearer the apex. Assert ordering (per UI-SPEC:
        ordering, not a specific curve);
    (5) ASSERT delete-transports-next: delete/remove the top candidate, re-query (or apply the
        delete-and-replace path), and assert the next-most-similar candidate now occupies the
        nearest-apex (vacated) slot (§O.18/§O.14).
    Factor the monotonicity + transport assertions into small pure helper fns (e.g.
    assert_cone_monotonic(candidates), assert_delete_transports_next(before, after)) so the
    --self-test can exercise them WITHOUT a real backend.
    Add a `--self-test` mode (run_self_test, mirroring duckduckgo's lines 396-465) that:
    (a) confirms the all_real gate FIRES — feeds a stub all_real:false status into assert_all_real
        and asserts it raises (the EXPECTED outcome);
    (b) confirms the assertion scaffold has TEETH — feeds a deliberately NON-monotonic candidate set
        into assert_cone_monotonic and asserts it RAISES, and a deliberately wrong delete-and-replace
        into assert_delete_transports_next and asserts it RAISES.
    --self-test exits 0 only when every teeth-check raised as expected; it requires NO real GPU and
    is the parse-plus-logic gate. Add an argparse `--backend` (default http://127.0.0.1:8080) and
    `--self-test` flag, and the run-as banner documenting the clean-GPU-preflight + main-context-only
    discipline (this probe is NEVER driven from a verifier subagent — a wedged CUDA/Selenium boot
    hangs it).
  </action>
  <verify>
    <automated>python scripts/probe_live_cone_transport.py --self-test --backend http://127.0.0.1:8080</automated>
  </verify>
  <done>probe_live_cone_transport.py exists; --self-test exits 0 (all_real gate fires + monotonicity/delete-transports assertions have teeth) without a real GPU; the real-run path asserts all_real, real-scan cone monotonicity in the real triple-product score, and delete-transports-next.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: D-01 real-subsystem cone-transport acceptance (main-context, blocking-human)</name>
  <action>BLOCKING-HUMAN checkpoint — not auto-executed. Run the real probe from the MAIN context per the how-to-verify steps below (clean-GPU preflight, real backend on :8080, all_real:true, then the real probe run), then await the resume-signal. See what-built / how-to-verify / resume-signal.</action>
  <verify>
    <human-check>The real probe (python scripts/probe_live_cone_transport.py --backend http://127.0.0.1:8080) exits 0 from the main context with all_real:true, cone placement monotonic in the real triple-product score, and delete-transports-next asserted.</human-check>
  </verify>
  <done>Human has run the real probe from the main context and approved (exit 0, all_real:true, monotonic + delete-transports-next), or recorded the failure.</done>
  <what-built>
    The D-01 real-subsystem cone-transport acceptance probe (scripts/probe_live_cone_transport.py)
    plus its --self-test stub gate. The probe asserts — against REAL nomic + TF-IDF over a real
    archive.org scan — that halo cone placement is monotonic in the real normalized triple-product
    score and that deleting the top result transports the next-most-similar in. This is the HALO-03
    acceptance proof; it is NOT auto-executed (a wedged CUDA/Selenium boot hangs an agent), so the
    real run is performed from the MAIN context with a clean-GPU preflight, exactly as Phase 7's
    07-06 Task 4 was.
  </what-built>
  <how-to-verify>
    From the MAIN context (NEVER a verifier subagent), per STATE.md env discipline:
    1. Clean-GPU preflight: confirm ~0 MiB VRAM and 0 stray python/firefox processes
       (`nvidia-smi`; `tasklist | grep -iE 'python|firefox'`). If any stray, `taskkill /F /T` them
       and re-confirm before booting.
    2. Boot the REAL backend on :8080 with no WFH_FAKE_* gates (real GPT4All + nomic + Selenium +
       LangGraph). Confirm GET /api/subsystem_status returns all_real:true.
    3. Run the FAST gate first (both modes): the stub cone e2e
       `npx playwright test frontend_e2e/halo.spec.js -g "cone"` + `env-scenario --name apparition-mode`
       + `--name halo-chain-roundtrip` + `--name full-smoke` (stub AND real). All green.
    4. Run the REAL probe from the main context:
       `python scripts/probe_live_cone_transport.py --backend http://127.0.0.1:8080`
       Confirm it reports: all_real:true; a real scan completed; cone placement MONOTONIC in the
       real normalized triple-product score; deleting the top result transported the
       next-most-similar in. Exit code 0.
    5. Tear down the real backend with `taskkill /F /T` and re-confirm a clean GPU.
    A screenshot is NOT acceptance proof — the probe's exit-0 + its printed assertions are.
  </how-to-verify>
  <resume-signal>Type "approved" once the real probe passes from the main context (exit 0, all_real:true, monotonic + delete-transports-next asserted), or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- `python scripts/probe_live_cone_transport.py --self-test --backend http://127.0.0.1:8080` — the stub gate exits 0 (all_real gate fires; monotonicity + delete-transports assertions have teeth); requires no real GPU.
- (Main-context, human-gated) `python scripts/probe_live_cone_transport.py --backend http://127.0.0.1:8080` — the real run passes: all_real:true, real-scan cone monotonicity in the real triple-product, delete-transports-next.
- The fast gate (stub cone e2e + apparition-mode/halo-chain-roundtrip + full-smoke both modes) stays green.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real Selenium scan → workspace | The probe triggers a REAL external scan (archive.org). This is the same boundary every existing probe_live_*.py already crosses; no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | a non-functional probe passing parse-only | mitigate | --self-test gate proves the assertion scaffold has teeth (a broken candidate set FAILS) before the real run is trusted |
| T-08-10 | Spoofing | stub backend masquerading as real | mitigate | assert_all_real fires on all_real:false; the real run requires /api/subsystem_status.all_real == True |
| T-08-SC | Tampering | npm/pip installs | accept | This plan adds zero new third-party packages (08-RESEARCH Package Legitimacy Audit: not applicable); no install task, no [ASSUMED]/[SUS] package |
</threat_model>

<success_criteria>
- `scripts/probe_live_cone_transport.py` exists with both the real run and the `--self-test` gate.
- `--self-test` exits 0 without a real GPU, proving the all_real gate fires and the assertions have teeth.
- The real run (main-context, human-gated) asserts all_real:true, real-scan cone monotonicity in the real triple-product, and delete-transports-next.
- The human checkpoint is approved.
</success_criteria>

<output>
Create `.planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-04-SUMMARY.md` when done.
</output>
