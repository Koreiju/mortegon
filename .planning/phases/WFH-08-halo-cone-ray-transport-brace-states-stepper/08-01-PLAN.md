---
phase: 08-halo-cone-ray-transport-brace-states-stepper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/static/js/fe/magic_markdown.mjs
  - backend/static/js/fe/magic_markdown_panel.mjs
  - backend/static/js/fe/magic_markdown.test.mjs
  - frontend_e2e/halo.spec.js
autonomous: true
requirements: [HALO-04]
tags: [halo, brace-states, render-wiring, frontend]

must_haves:
  truths:
    - "A braced-hidden {ref} renders a ▸ glyph and keeps its literal braces in panel form (per D-02)"
    - "A revealed-internal {ref} renders a ▾ glyph and its rank-1 children inline (per D-02)"
    - "A resolved-external {ref} draws a SOLID cross-ref line (no dasharray) to the already-visible target node in graph form (per D-02, closes Phase-7 gap)"
    - "Panel form and graph form show the SAME node count when a ref is revealed (node-count parity, per D-02 / §O.1a)"
  artifacts:
    - path: "backend/static/js/fe/magic_markdown.mjs"
      provides: "renderGraph threads braceState onto each graph node"
      contains: "braceState: l.braceState"
    - path: "backend/static/js/fe/magic_markdown_panel.mjs"
      provides: "panelVDom + graphVDom branch on braceState; graphVDom draws the resolved-external solid link"
    - path: "backend/static/js/fe/magic_markdown.test.mjs"
      provides: "unit cases for braceState threading + panel/graph render branching"
    - path: "frontend_e2e/halo.spec.js"
      provides: "brace-state e2e: two-level ref chain, node-count parity, no-dasharray"
  key_links:
    - from: "backend/static/js/fe/magic_markdown_panel.mjs"
      to: "backend/static/js/fe/magic_markdown.mjs"
      via: "panelVDom/graphVDom read line.braceState computed by classifyBraceStates"
      pattern: "braceState"
    - from: "backend/static/js/fe/magic_markdown.mjs::renderGraph"
      to: "backend/static/js/fe/magic_markdown.mjs::classifyBraceStates"
      via: "renderGraph node shape carries braceState from the classified lines"
      pattern: "braceState: l\\.braceState"
---

<objective>
Wire Phase 7's already-correct `classifyBraceStates` classification INTO the panel and graph
renderers so the three `{ref}` render states become VISUAL (HALO-04, D-02). This closes the
Phase-7 self-flagged gap (07-03 SUMMARY: "classifyBraceStates computed but panelVDom/graphVDom
ignore l.braceState; resolved-external solid link has no graphVDom drawing").

This is the smallest, most self-contained plan in the phase — pure render-layer wiring over an
existing, tested classification. No backend, no new module, no 3D geometry. It runs first
(Wave 1) so the brace-state surface is solid before the cone (Wave 2) and stepper (Wave 3) build.

Purpose: Realize the three `{ref}` render states (§O.1/§O.1a/§O.2) with panel↔graph node-count
parity — the second of the three Phase-8 requirements, and the one with zero new computation.
Output: braceState threaded through `renderGraph`; `panelVDom`/`graphVDom` branch on it;
`graphVDom` draws the resolved-external SOLID cross-ref line; unit + e2e coverage.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-UI-SPEC.md
@.planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan (08-01) produces:
- `backend/static/js/fe/magic_markdown.mjs` — `renderGraph` node shape extended with `braceState`
- `backend/static/js/fe/magic_markdown_panel.mjs` — `panelVDom`/`graphVDom` branch on `braceState`; resolved-external solid link draw
- `backend/static/js/fe/magic_markdown.test.mjs` — extended unit cases (this is the Wave-0 unit scaffold for HALO-04)
- `frontend_e2e/halo.spec.js` — new brace-state e2e cases (Open-Q4 resolved: ALL Phase-8 e2e land in `halo.spec.js`)

Phase-wide artifacts produced by sibling plans (for reference): `fe/halo_cone.mjs` (08-02),
`fe/stepper.mjs` + `projector.flyToNode`/`highlightNode` + signal-cursor chunk-id field (08-03),
the D-01 real probe + `--self-test` (08-04).
</artifacts_this_phase_produces>

<resolved_open_questions>
- **Open-Q3 (resolved-external link layer) — RESOLVED to the 2D in-graph `<line>`.** Both endpoints
  of a resolved-external link are graph-mode children of the SAME panel expansion (the referring
  line and the already-revealed target line both live in `graphVDom`'s own SVG namespace). Use the
  plain in-graph `<line>` already drawn by `graphVDom` for containment edges, swapping only the
  stroke color. Do NOT reach for `projector.mjs::drawConcept3DLinks` — that 3D-aware variant is for
  links terminating at a `data-3d-node-id` 3D node, which is not this case. (Per 08-RESEARCH Open
  Question 3 / 08-PATTERNS Open Question 3 — default to the simpler 2D idiom.)
- **Open-Q4 (e2e file placement) — RESOLVED to `halo.spec.js`.** All Phase-8 e2e (brace states here,
  cone in 08-02, stepper in 08-03) land in `frontend_e2e/halo.spec.js` — topically exact, and the
  existing `__mm_halo_*` test hooks already live there. (Per 08-RESEARCH Pitfall 4 / Open Question 4.)
</resolved_open_questions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread braceState through renderGraph and add the missing unit cases</name>
  <read_first>
    - backend/static/js/fe/magic_markdown.mjs lines 28-36 (GLYPH_COLLAPSED/EXPANDED + BRACE_HIDDEN/REVEALED_INTERNAL/RESOLVED_EXTERNAL constants), 259-281 (classifyBraceStates — the CORRECT half, do NOT modify its logic), 299-320 (renderGraph — the half to extend; line 301-310 node shape is missing braceState), 490-495 (the module export list)
    - backend/static/js/fe/magic_markdown.test.mjs (existing iterableNode per-signal test shape — the structural precedent for the new per-state cases)
    - .planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-PATTERNS.md lines 126-163 (the exact renderGraph gap + classifyBraceStates output shape to consume verbatim)
  </read_first>
  <behavior>
    - Test 1: classifyBraceStates over lines containing one expanded ref and one collapsed ref to the SAME target marks the expanded line BRACE_REVEALED_INTERNAL and the collapsed sibling BRACE_RESOLVED_EXTERNAL (regression-guard the existing classification stays intact).
    - Test 2: renderGraph's returned nodes each carry a braceState key equal to the corresponding line's braceState (the Pitfall-1 fix — currently the key is absent).
    - Test 3: a node with no ref (refTarget == null) has braceState === undefined (no spurious state on plain field lines).
    - Test 4: node-count parity — renderGraph(node).nodes.length === renderPanel(node).length for a tree containing braced-hidden, revealed-internal, and resolved-external refs (parity holds across all three states).
  </behavior>
  <action>
    In renderGraph (magic_markdown.mjs ~line 301-310), add `braceState: l.braceState` to the
    object literal built per line inside `lines.map(...)`. This is the entire Pitfall-1 fix per
    08-RESEARCH Pitfall 1 and 08-PATTERNS lines 142-148: classifyBraceStates already runs inside
    renderPanel (whose output renderGraph consumes via `const lines = renderPanel(...)`), so every
    line already carries `.braceState` — renderGraph was simply dropping it when destructuring the
    graph-node shape. Do NOT touch classifyBraceStates' logic (it is correct and Phase-7-tested);
    do NOT add new edge types (§O.1a: one invariant ConceptEdge, three RENDER states — adding
    BRACED_EDGE/REVEALED_EDGE/RESOLVED_EDGE is the forbidden anti-pattern per 08-RESEARCH).
    Then extend magic_markdown.test.mjs with the four <behavior> cases above, mirroring the
    existing per-signal test structure in that file. Use the exported BRACE_HIDDEN /
    BRACE_REVEALED_INTERNAL / BRACE_RESOLVED_EXTERNAL constants (lines 34-36) for assertions, never
    string literals, so a constant rename can never silently pass.
  </action>
  <verify>
    <automated>node --test backend/static/js/fe/magic_markdown.test.mjs</automated>
  </verify>
  <done>renderGraph nodes carry braceState matching classifyBraceStates; four new unit cases pass; node-count parity holds across all three brace states; classifyBraceStates logic unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch panelVDom/graphVDom on braceState and draw the resolved-external solid link</name>
  <read_first>
    - backend/static/js/fe/magic_markdown_panel.mjs lines 22 (imports from magic_markdown.mjs — add the BRACE_* constants here), 41-76 (panelVDom — the glyph-only branch to extend), 85-125 (graphVDom — the edges/lineEls construction to extend with the resolved-external link; lines 108-114 is the existing containment-edge draw to mirror), 280-289 (the existing transient SOLID drag line — the no-dasharray idiom precedent in THIS file)
    - backend/static/js/fe/magic_markdown.mjs lines 28-36 (glyph + brace constants to import)
    - .planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-UI-SPEC.md lines 126-149 (Interaction Contract B glyph table: BRACE_HIDDEN → ▸ --silver-700; BRACE_REVEALED_INTERNAL → ▾ --silver-300; BRACE_RESOLVED_EXTERNAL → no glyph, draw a solid link; link color --accent-arrow if 3D-backed else --silver-300)
    - .planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-PATTERNS.md lines 167-211 (panelVDom/graphVDom analog excerpts + the no-dasharray stroke contract)
  </read_first>
  <behavior>
    - Test 1: panelVDom over a braced-hidden ref line emits the ▸ glyph and retains the literal braces in the text span.
    - Test 2: panelVDom over a revealed-internal ref line emits the ▾ glyph (the existing inline-children path is unchanged; assert the glyph selection is state-driven).
    - Test 3: graphVDom over a tree with a resolved-external ref emits an extra <line> element whose endpoints are the referring node and the already-revealed target node, and that <line> has NO stroke-dasharray attribute.
    - Test 4: the resolved-external <line> uses stroke --accent-arrow (#ffd700) only when the target node carries data-3d-node-id, else --silver-300 (assert via a flattenVDom scan for the line's stroke attr).
    - Test 5 (parity): graphVDom and panelVDom render the same number of field nodes/lines for the same tree (no extra phantom nodes introduced by the link draw — the link is an edge, not a node).
  </behavior>
  <action>
    In magic_markdown_panel.mjs, import BRACE_HIDDEN / BRACE_REVEALED_INTERNAL /
    BRACE_RESOLVED_EXTERNAL from magic_markdown.mjs (extend the existing import at line 22).
    In panelVDom (lines 43-68): select the glyph from l.braceState rather than the raw l.glyph
    truthy check — BRACE_HIDDEN → the GLYPH_COLLAPSED "▸" character; BRACE_REVEALED_INTERNAL →
    GLYPH_EXPANDED "▾" (the existing inline-children expansion path stays exactly as-is; only the
    glyph SELECTION becomes state-aware). Keep the literal braces in the text span for
    BRACE_HIDDEN; the existing renderPanel already drops braces on expansion for the revealed line.
    In graphVDom (lines 108-114): after the existing containment-edge `lineEls` map, append a
    SECOND set of <line> elements — one per node whose braceState === BRACE_RESOLVED_EXTERNAL —
    drawn from that node's position to the position of the node whose path is the already-revealed
    target (find the revealed-internal node sharing the same refTarget; reuse the `pos` Map already
    built at line 88-95). Stroke color: "var(--accent-arrow,#ffd700)" when the target node carries
    a data-3d-node-id (per UI-SPEC + Open-Q3 resolution), else "var(--silver-300,#b8c0c8)";
    stroke-width "2"; opacity "0.85". NEVER set stroke-dasharray (hard project-wide rule — the
    black_slate.spec.js no-dotted gate must stay green). NEVER set marker-end (headless connector,
    §O.16). This reuses the EXACT in-graph <line> idiom graphVDom already uses for containment
    edges — only the stroke color and the endpoint selection differ. Extend magic_markdown.test.mjs
    (or add panel-specific cases if a magic_markdown_panel.test.mjs exists; check during read) with
    the five <behavior> cases via flattenVDom attribute scans — no DOM/browser needed.
  </action>
  <verify>
    <automated>node --test backend/static/js/fe/magic_markdown.test.mjs</automated>
  </verify>
  <done>panelVDom selects ▸/▾ by braceState; graphVDom draws the resolved-external solid link (no dasharray, no marker-end, color per UI-SPEC); all five unit cases pass; node/line parity holds.</done>
</task>

<task type="auto">
  <name>Task 3: Brace-state e2e — two-level ref chain, node-count parity, no-dasharray gate</name>
  <read_first>
    - frontend_e2e/halo.spec.js (existing HALO-01/02 coverage + the __mm_halo_rotate test-hook pattern + the in-page dynamic import() convention against the live served origin — mirror this exact structure)
    - frontend_e2e/object_exploration.spec.js (Phase-7's brace-state model-level e2e on l.braceState — the structural precedent for a two-level ref chain; do NOT duplicate it, build the RENDER-level assertion here)
    - frontend_e2e/black_slate.spec.js (the no-dotted/no-dasharray gate this plan must not regress — read its assertion shape)
    - .planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-UI-SPEC.md lines 144 (node-count parity is the e2e acceptance gate), 197-199 (verification idiom: e2e/scenario, never screenshots)
  </read_first>
  <action>
    Add a `test.describe("brace render states (HALO-04)")` block to halo.spec.js (Open-Q4 resolved:
    Phase-8 e2e lands here). Drive the served fe/ modules via in-page dynamic import() against the
    live served origin (the Phase-7 object_exploration.spec.js convention — do NOT modify demo.html
    or the panel module to expose hooks). Build a two-level ref chain (a root concept whose field
    refs concept B, whose field refs concept C) and assert, against the real rendered DOM:
    (1) braced-hidden refs show the ▸ glyph with literal braces; (2) revealing a ref in panel form
    swaps it to ▾ and reveals the SAME rank-1 children that appear in graph form (node-count parity
    — count `.mm-line` in panel vs `.mm-gnode` in graph for the revealed subtree, assert equal);
    (3) a second ref to an already-revealed target renders as a resolved-external <line> in graph
    mode with NO stroke-dasharray; (4) querySelectorAll('[stroke-dasharray]') within the graph SVG
    returns zero elements (the no-dotted gate, mirrored from black_slate.spec.js). Use the
    ROADMAP success-criterion-2 framing ("an e2e over a two-level ref chain") as the acceptance bar.
  </action>
  <verify>
    <automated>npx playwright test frontend_e2e/halo.spec.js -g "brace"</automated>
  </verify>
  <done>The two-level-ref-chain e2e asserts ▸/▾ glyph selection, panel↔graph node-count parity on reveal, the resolved-external solid link, and zero stroke-dasharray in graph mode; black_slate.spec.js still green.</done>
</task>

</tasks>

<verification>
- `node --test backend/static/js/fe/magic_markdown.test.mjs` — all brace-state unit cases pass.
- `npx playwright test frontend_e2e/halo.spec.js -g "brace"` — the two-level ref chain e2e passes.
- `npx playwright test frontend_e2e/black_slate.spec.js` — the no-dotted/no-dasharray gate stays green (this plan adds a SOLID link; it must not introduce any dasharray anywhere).
- `python scripts/sim_frontend.py --backend http://127.0.0.1:8080 env-scenario --name full-smoke` — green in BOTH stub and real modes (per the standing contract).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is render-layer-only; it adds no new endpoint, no new user-input parse, no new external-data path. classifyBraceStates already runs over existing parsed lines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | resolved-external link stroke (could regress to dashed) | mitigate | e2e asserts zero stroke-dasharray in graph mode (Task 3); black_slate.spec.js gate stays green |
| T-08-02 | Information Disclosure | brace-state render | accept | No PII; the render reveals only already-loaded concept fields the user authored |
</threat_model>

<success_criteria>
- braceState is threaded through renderGraph and consumed by both panelVDom and graphVDom.
- The three render states are VISUAL: ▸ + braces (hidden), ▾ + inline children (revealed-internal), SOLID cross-ref link (resolved-external).
- Panel↔graph node-count parity holds on ref reveal (asserted by e2e).
- No stroke-dasharray anywhere; black_slate.spec.js stays green.
- The Phase-7 self-flagged gap is closed.
</success_criteria>

<output>
Create `.planning/phases/WFH-08-halo-cone-ray-transport-brace-states-stepper/08-01-SUMMARY.md` when done.
</output>
