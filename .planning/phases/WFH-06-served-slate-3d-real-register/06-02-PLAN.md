---
phase: 06-served-slate-3d-real-register
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/static/js/fe/projector.mjs
  - backend/templates/editor.html
  - frontend_e2e/projector.spec.js
autonomous: true
requirements: [REAL-02]
must_haves:
  truths:
    - "Each URL's chunks cluster around that URL's backend-emitted root_position; a second URL's chunks land non-overlapping"
    - "Re-scanning the first URL does not move the second URL's root (old roots never move)"
    - "On scan-end the camera tweens to frame the newest URL's root, unless the user interacted AND the root is already in frustum"
    - "url_roots from umap_canonical (WS) and from /api/recompute_umap (boot) both reach the projector — no longer dropped"
  artifacts:
    - path: "backend/static/js/fe/projector.mjs"
      provides: "frameCameraToRoot tween + adaptive camera minDistance/maxDistance bounds; newest-root bookkeeping"
      contains: "frameCameraToRoot"
    - path: "backend/templates/editor.html"
      provides: "WS + boot pass url_roots into setNodes(coords, urlRoots)"
      contains: "url_roots"
    - path: "frontend_e2e/projector.spec.js"
      provides: "multi-scan e2e block (non-overlap + old-root-stable + camera frame)"
      contains: "multi-scan"
  key_links:
    - from: "backend/templates/editor.html"
      to: "backend/static/js/fe/projector.mjs"
      via: "WS umap_canonical handler + bootProjector fetch pass f.url_roots / res.url_roots into setNodes"
      pattern: "setNodes\\([^,]+, *[a-z_]*url_roots"
    - from: "backend/static/js/fe/projector.mjs"
      to: "backend/api/ws_frames.py"
      via: "consume url_roots {url:{root_position,bounding_radius}} — never recompute placement (D10, A2)"
      pattern: "root_position"
---

<objective>
Port REAL-02 — per-URL multi-scan placement consumption + camera framing — into the
served projector. The backend (`layout_service.py`/`build_umap_canonical`) ALREADY
computes and broadcasts `url_roots` (per-URL `root_position` + `bounding_radius`);
the entire gap is that `editor.html` drops `f.url_roots` today (line 296) and the
projector never frames the camera. This plan wires `url_roots` through both the WS
`umap_canonical` handler and the `bootProjector()` REST path into the
`setNodes(coords, urlRoots)` signature added in Plan 01, then adds the camera
auto-framing tween + adaptive orbit bounds.

Purpose: closes the visible REAL-02 gap (two scans currently land both clusters near
the origin because url_roots never reached the ray/camera math). The frontend's job
is strictly to CONSUME the backend's resolved roots and frame the camera — never to
recompute placement (D10; Assumption A2: zero client-side SAFETY_GAP constant).
Output: `projector.mjs` gains `frameCameraToRoot(url)` (cubic-ease tween to a root)
+ per-frame adaptive `minDistance`/`maxDistance` bounds + newest-root tracking;
`editor.html` passes `url_roots` on WS and boot; `projector.spec.js` gains the
`multi-scan` e2e block.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-06-served-slate-3d-real-register/06-RESEARCH.md
@.planning/phases/WFH-06-served-slate-3d-real-register/06-PATTERNS.md
@.planning/phases/WFH-06-served-slate-3d-real-register/06-UI-SPEC.md
@.planning/phases/WFH-06-served-slate-3d-real-register/06-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols/attributes this plan authors (excluded from drift verification by downstream review):
- `frameCameraToRoot(url)` — closure-internal camera tween in `createProjector()`
- `_newestUrl` tracking + `_userInteracted` flag (OrbitControls 'start' listener) + `_applyCameraBounds()` per-frame
- Camera bound constants: `minDistance = 0.6 × cluster_radius`, `maxDistance = 3.0 × max(|pos|)` (UI-SPEC-locked; ported verbatim from cp/animation.js lines 765/794)
- `editor.html`: `f.url_roots` (WS) and `res.url_roots` (boot) passed into `setNodes`; `window.__mm_proj_camera_distance()` test hook
- `projector.spec.js` new `test("...multi-scan...")` block
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire url_roots through the WS handler + boot fetch (close the drop)</name>
  <files>backend/templates/editor.html</files>
  <read_first>
    - backend/templates/editor.html (line 296 WS handler `if (f.type === "umap_canonical" ... ) projector.setNodes(f.coords)` — drops f.url_roots; bootProjector() lines 327-359 fetches /api/recompute_umap and calls setNodes(coords) without url_roots)
    - backend/api/ws_frames.py (build_umap_canonical wire contract: `{ coords, url_roots:{url:{root_position:[x,y,z], bounding_radius:r}}, removed_ids, provenance }`)
    - backend/static/js/fe/projector.mjs (setNodes(coords, urlRoots) signature from Plan 01)
    - .planning/phases/WFH-06-served-slate-3d-real-register/06-RESEARCH.md (Pitfall 1 — both the WS handler AND the bootProjector REST path must pass url_roots)
  </read_first>
  <action>
    In `editor.html`, change the WS `umap_canonical` branch to pass `f.url_roots` as the second arg: `projector.setNodes(f.coords, f.url_roots)`. In `bootProjector()`, change `projector.setNodes(coords)` to `projector.setNodes(res.coords || {}, res.url_roots || {})` (the `/api/recompute_umap` response already returns `url_roots` via `build_umap_canonical`). Do NOT recompute or synthesize roots client-side — pass exactly what the backend emits (D10; A2 — no client SAFETY_GAP). Add a `window.__mm_proj_camera_distance = () => projector.camera.position.length()` test hook in `bootProjector()` alongside the existing `__mm_proj_*` hooks (used by the camera-frame assertion in Task 3).
  </action>
  <verify>
    <automated>npx playwright test projector.spec.js -g "UMAP-01"</automated>
  </verify>
  <done>The WS handler and boot fetch both forward url_roots; the existing UMAP-01 render test still passes (single-arg backward-compat preserved by Plan 01's optional second arg).</done>
  <acceptance_criteria>
    - `editor.html` WS branch calls `setNodes(f.coords, f.url_roots)` (grep shows url_roots passed)
    - `bootProjector()` passes `res.url_roots` into setNodes
    - No client-side placement recomputation added (no `SAFETY_GAP`, no golden-angle candidate search, no `_computeUrlRootPosition` port in `projector.mjs` or `editor.html`)
    - `npx playwright test projector.spec.js -g "UMAP-01"` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Camera auto-framing tween + adaptive orbit bounds</name>
  <files>backend/static/js/fe/projector.mjs</files>
  <read_first>
    - backend/static/js/fe/projector.mjs (createProjector: existing camera at (0,0,140), OrbitControls conditional init, animate() loop, the _urlRootPositions/_urlBoundingRadii Maps + newest-url bookkeeping from Plan 01)
    - backend/static/js/cp/animation.js (camera framing/tween reference: adaptive bounds lines 753-798 — minDistance = 0.6 × cluster_radius, maxDistance = 3.0 × max(|pos|); cubic-ease flyToNode/frameAllInstances)
    - .planning/phases/WFH-06-served-slate-3d-real-register/06-UI-SPEC.md (REAL-02 section: bounds formulas LOCKED; tween suppressed if user interacted AND root already in frustum; adaptive resize via ResizeObserver with no no-change guard)
    - .planning/phases/WFH-06-served-slate-3d-real-register/06-PATTERNS.md (REAL-02 — consume never recompute; camera adaptive-bounds verbatim, UI-SPEC is the numeric authority)
  </read_first>
  <action>
    Add closure-internal `frameCameraToRoot(url)` to `projector.mjs`: read the url's `root_position` (THREE.Vector3) + `bounding_radius` from `_urlRootPositions`/`_urlBoundingRadii`; tween the camera target (OrbitControls `target`) and position to frame that root over `~600ms` with a cubic ease (port the cubic-ease step from `cp/animation.js`; interruptible). In `setNodes`, when a NEW url first appears (Plan 01's newUrls bookkeeping), record it as `_newestUrl` and call `frameCameraToRoot(_newestUrl)` ON SCAN-END framing — but SUPPRESS the tween if `_userInteracted === true` AND the root is already within the camera frustum (project the root via the existing `project()`/NDC z-range check). Track `_userInteracted` by adding an OrbitControls `'start'` event listener that sets it true (guard the listener on `controls` existing). Add a per-frame `_applyCameraBounds()` called inside `animate()` that recomputes `controls.minDistance = 0.6 * clusterRadius` (clusterRadius = max bounding_radius over visible roots, fallback to a sane default) and `controls.maxDistance = 3.0 * maxAbsPos` (max |position| over all current node world positions); apply only when `controls` exists. Replace/augment the existing `resize()` to ALSO attach a `ResizeObserver` on the canvas that calls `renderer.setSize(w,h,false)` + `camera.updateProjectionMatrix()` with NO no-change guard (UI-SPEC "Adaptive resize"). These are render-loop concerns (Browser/Client tier) — they consume backend roots, never recompute them (D10).
  </action>
  <verify>
    <automated>node --test backend/static/js/fe/projector.test.mjs</automated>
  </verify>
  <done>frameCameraToRoot tweens to the newest root on scan-end (suppressed correctly when the user has interacted and the root is framed); adaptive bounds + ResizeObserver are live; unit tests + the prior force-directed e2e remain green.</done>
  <acceptance_criteria>
    - `projector.mjs` contains `frameCameraToRoot` and applies `minDistance = 0.6 ×` / `maxDistance = 3.0 ×` per the UI-SPEC formulas (grep shows `0.6` and `3.0` multipliers)
    - A ResizeObserver is attached to the canvas (no no-change guard)
    - The tween-suppress condition checks BOTH user-interaction AND in-frustum (not just one)
    - `node --test backend/static/js/fe/projector.test.mjs` exits 0; `npx playwright test projector.spec.js -g "force-directed"` still passes
    - No `SAFETY_GAP`/root-placement recomputation introduced
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: multi-scan e2e — non-overlap + old-root-stable + camera frames newest</name>
  <files>frontend_e2e/projector.spec.js</files>
  <read_first>
    - frontend_e2e/projector.spec.js (existing skip-gate idiom + the __mm_proj_set_with_roots / __mm_proj_node_positions hooks from Plan 01 + __mm_proj_camera_distance from Task 1)
    - backend/static/js/fe/projector.mjs (frameCameraToRoot, newest-root bookkeeping)
    - .planning/phases/WFH-06-served-slate-3d-real-register/06-VALIDATION.md (REAL-02 row: e2e + perimeter-rescale REPL; multi-scan non-overlap + old roots never move + camera frames newest)
  </read_first>
  <action>
    Add a `test("REAL-02 multi-scan: per-URL clusters land non-overlapping, old roots never move, camera frames newest", ...)` block to `projector.spec.js` (skip-gate idiom). In `page.evaluate`: (1) inject scan A — `__mm_proj_set_with_roots(coordsA, { 'urlA': { root_position:[0,0,0], bounding_radius:6 } })`; record nodeA positions via `__mm_proj_node_positions()` and the camera distance via `__mm_proj_camera_distance()`. (2) inject scan B — a SECOND url at a backend-style non-overlapping root (e.g. `root_position:[40,0,0], bounding_radius:6`) merged with the still-present urlA coords; advance a few frames. Assert: (a) urlB's chunk cluster centroid is separated from urlA's by at least `boundingA + boundingB` (non-overlap — the backend already encodes `existing_max + new_radius + safety_gap`; the e2e just confirms the frontend renders both clusters at their backend roots rather than at the origin); (b) urlA's chunk positions are unchanged (within tolerance) between step 1 and step 2 — old roots never move; (c) the camera framed toward the newest root — assert the camera position/target shifted toward urlB's root after scan B (distance or target.x moved). Keep all reads behind the WebGL skip gate.
  </action>
  <verify>
    <automated>npx playwright test projector.spec.js -g "multi-scan"</automated>
  </verify>
  <done>The multi-scan e2e proves both URL clusters render at their backend roots (non-overlapping), the first URL's chunks do not move on the second scan, and the camera frames the newest root — or deterministically skips when WebGL is unavailable.</done>
  <acceptance_criteria>
    - `npx playwright test projector.spec.js -g "multi-scan"` passes or skips deterministically
    - The non-overlap assertion uses the backend-emitted bounding_radii (no client-recomputed gap)
    - An assertion confirms urlA positions are stable across the second scan
    - `python scripts/sim_frontend.py env-scenario --name perimeter-rescale` stays green (backend placement contract)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend WS/REST → editor.html → projector | `url_roots` crosses from server placement math into client camera/ray state |
| OrbitControls user input → camera state | user orbit gestures gate the auto-frame tween |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | a url_roots entry missing root_position/bounding_radius | mitigate | frameCameraToRoot guards on the Map having the url; bounds fall back to sane defaults when no roots present; force-step ray fallback (Plan 01) covers the chunk side |
| T-06-05 | Repudiation | camera tween fights the user's orbit | mitigate | the suppress-if-interacted-AND-in-frustum condition (UI-SPEC) prevents the auto-frame from overriding deliberate user navigation |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages this plan |
</threat_model>

<verification>
- `npx playwright test projector.spec.js -g "multi-scan"` — non-overlap + old-root-stable + camera frame
- `npx playwright test projector.spec.js -g "UMAP-01|force-directed"` — no regression
- `python scripts/sim_frontend.py env-scenario --name perimeter-rescale` + `--name 6d-umap-format` — backend placement contract stays green (both modes)
- `npm run test:e2e` at wave merge
</verification>

<success_criteria>
- url_roots reaches the projector on both WS and boot paths (the line-296 drop is closed)
- Two scans render at their distinct backend roots non-overlapping; old roots never move; the camera frames the newest root unless the user is navigating a framed root
- Camera bounds use the UI-SPEC-locked 0.6×/3.0× multipliers; adaptive ResizeObserver is live; zero client-side placement recomputation (D10/A2)
</success_criteria>

<output>
Create `.planning/phases/WFH-06-served-slate-3d-real-register/06-02-SUMMARY.md` when done
</output>
