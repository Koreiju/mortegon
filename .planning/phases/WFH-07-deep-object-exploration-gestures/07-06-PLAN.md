---
phase: 07-deep-object-exploration-gestures
plan: 06
type: execute
wave: 5
depends_on: ["07-05"]
files_modified:
  - scripts/sim_frontend.py
  - scripts/probe_live_duckduckgo_walkthrough.py
  - frontend_e2e/object_exploration.spec.js
autonomous: false
requirements: [EXPLORE-04]
user_setup:
  - service: clean-GPU preflight
    why: "D-01 mandates the real DuckDuckGo run boots against a clean GPU (0 stray python/firefox, ~0 VRAM); a wedged CUDA/Selenium state hangs the real backend boot indefinitely"
must_haves:
  truths:
    - "A duckduckgo-walkthrough REPL env-scenario runs the §N flow and is green in BOTH stub and real modes"
    - "probe_live_duckduckgo_walkthrough.py asserts all_real:true, a real Selenium DuckDuckGo scan, and a real materialiser type graph"
    - "The §N flow holds end-to-end: author self=duckduckgo referencing scan, drag-wire WebBrowser scanner, reveal rank-1 url{}/dom{}, per-sample chunk iteration on {chunk samples}"
  artifacts:
    - path: "scripts/sim_frontend.py"
      provides: "duckduckgo-walkthrough env-scenario (stub fast-gate, modeled on _env_scenario_node_fold_roundtrip)"
      contains: "duckduckgo"
    - path: "scripts/probe_live_duckduckgo_walkthrough.py"
      provides: "live real-subsystem probe (real Selenium scan + real materialiser, all_real assertion)"
      contains: "all_real"
  key_links:
    - from: "duckduckgo-walkthrough scenario"
      to: "editor-create + drag-wire(editor-link inherit_types) + ui-node-fold + {chunk samples} iterable"
      via: "the N-flow gesture sequence driven through the existing REPL verbs + Plan 04's inherit_types"
      pattern: "editor-create|node-fold"
    - from: "probe_live_duckduckgo_walkthrough.py"
      to: "/api/subsystem_status all_real + WebBrowser scan WS pipeline"
      via: "confirm all_real:true before asserting; real Selenium DuckDuckGo scan watched through done"
      pattern: "subsystem_status|all_real"
---

<objective>
Build EXPLORE-04: the DuckDuckGo §N walkthrough end-to-end. Per D-01 this is verified REAL-SUBSYSTEM INLINE this phase — a duckduckgo-walkthrough REPL env-scenario (the stub fast-gate that stays green BOTH modes) AND a probe_live_duckduckgo_walkthrough.py (the real-subsystem acceptance proof on top).

Purpose: The §N walkthrough is the canonical acceptance example (object_exploration.md §5.1): begin in graph form, left-drag the WebBrowser scanner onto DuckDuckGo (inherits I/O types + object model, N.4), final panel presents NO types (DuckDuckGo / {scanner}), right-click {scanner} reveals type-stripped rank-1 url{}/dom{}, the call completes to a ShadowDOM whose full per-sample distribution lives in 3D while the 2D node stays rank-1, and chunk {chunk samples} per-sample iteration fires only when a downstream link externally references the recursively-chunked iterable (N.9). This plan composes Plans 01-05 into the lodestar flow and proves it against real Selenium + real materialiser.
Output: the REPL scenario + the live probe + the EXPLORE-04 e2e case, with the clean-GPU-gated real run.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-PATTERNS.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: duckduckgo-walkthrough REPL env-scenario (stub fast-gate, both modes)</name>
  <read_first>
    - scripts/sim_frontend.py lines ~7780-7806 (_env_scenario_node_fold_roundtrip — the EXACT template: purge, editor-create, ui-node-fold, assert against ui-state; the scenario-registration convention)
    - scripts/sim_frontend.py _act_editor_create / _act_editor_link / _act_editor_delete / _act_ui_dominance_collapse (the REPL verbs to compose; check whether editor-link needs an inherit_types arg added to drive Plan 04's N.4 side-effect, or whether a new ui-wire-link verb is cleaner)
    - backend/static/js/fe/magic_markdown.mjs lines 45-66 (iterableNode / isIterable / advanceSignal — the N.9 {chunk samples} per-sample iteration model the scenario asserts against)
    - .planning/phases/WFH-07-deep-object-exploration-gestures/07-CONTEXT.md specifics section (the canonical §N walkthrough step sequence)
  </read_first>
  <action>
    Add a duckduckgo-walkthrough env-scenario to scripts/sim_frontend.py, modeled exactly on _env_scenario_node_fold_roundtrip and registered the same way. Sequence: purge (confirm=erase); editor-create authoring self=duckduckgo referencing a scan ref; a drag-wire-equivalent step that wires the WebBrowser scanner onto DuckDuckGo with type inheritance (extend the editor-link REPL verb with an inherit_types arg that sets Plan 04's flag, OR add a ui-wire-link verb if cleaner — choose and document); assert the DuckDuckGo node now presents NO types (rank-1 minimalism: its rendered field text has no colon-type slot) and carries the inherited scanner; ui-node-fold to reveal rank-1 url{}/dom{}; assert via ui-state that the reveal recorded the expected expanded_paths AND the revealed fields are type-stripped; drive the {chunk samples} iterable (advanceSignal model) and assert per-sample iteration advances. In STUB mode (WFH_FAKE_SLM=1/WFH_FAKE_EMBEDDER=1/NO_WEBDRIVER=1) this must pass as the fast gate; the same scenario runs in real mode in Task 2's probe context. Register it so the env-scenario runner and full-smoke both pick it up. Do NOT spawn a parallel log stream — extend the existing watch-activity rows if new telemetry is needed (CLAUDE.md invariant #4).
  </action>
  <verify>
    <automated>WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python -m scripts.sim_frontend env-scenario --name duckduckgo-walkthrough --backend http://127.0.0.1:8080</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'duckduckgo' scripts/sim_frontend.py` shows the scenario defined AND registered in the env-scenario dispatch table
    - `env-scenario --name duckduckgo-walkthrough` exits 0 in STUB mode (the fast gate)
    - The scenario asserts the DuckDuckGo node is type-stripped (no colon-type slot) after the scanner is wired (rank-1 minimalism)
    - `env-scenario --name full-smoke` (stub) stays green with the new scenario included
  </acceptance_criteria>
  <done>duckduckgo-walkthrough env-scenario exists, is registered, and passes in stub mode driving the full §N sequence (author, drag-wire-with-inheritance, reveal rank-1 url{}/dom{} type-stripped, per-sample chunk iteration); full-smoke stays green.</done>
</task>

<task type="auto">
  <name>Task 2: probe_live_duckduckgo_walkthrough.py (real Selenium + real materialiser, all_real)</name>
  <read_first>
    - scripts/probe_live_archive_scan.py (the real-Selenium-scan pattern: per-snapshot WS watch through done, /api/subsystem_status all_real check before asserting, real chunk assertions — the structural template)
    - scripts/probe_python_api.py (the materialiser type-graph assertion style — to assert the real materialiser produced the WebBrowser scanner's typed url{}/dom{} rank-1)
    - .planning/STATE.md Blockers/Concerns (the clean-GPU env hygiene blocker + backend 8080 vs REPL 8000 + taskkill /F /T teardown)
    - .planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md Pitfall 5 (wedging CUDA/Selenium — drive the real backend from the MAIN context, never a verifier subagent)
  </read_first>
  <action>
    Create scripts/probe_live_duckduckgo_walkthrough.py modeled on probe_live_archive_scan.py. It MUST: (1) GET /api/subsystem_status and assert all_real:true before doing anything (loud failure if not — no stub fallback, per the no-mocks contract); (2) trigger a REAL Selenium DuckDuckGo scan via the existing scan-trigger route and watch the per-snapshot WS through done (real chunks land); (3) author self=duckduckgo and drag-wire the real WebBrowser scanner with inherit_types so the target inherits the real materialiser's typed url{}/dom{} (assert at least one real typed field is inherited, asserting against the real OBJECT_HAS_*/FUNCTION_*_TYPE edges the way probe_python_api.py does); (4) reveal rank-1 url{}/dom{} and assert they are type-stripped in the rendered form; (5) assert the {chunk samples} per-sample iteration advances over the REAL scanned chunk distribution (a real ShadowDOM + at least one real chunk). Honor the env discipline: this probe is run from the MAIN context after the clean-GPU preflight; use --backend http://127.0.0.1:8080. Do NOT add a stub fallback inside the probe — a failed real boot must surface loudly.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/probe_live_duckduckgo_walkthrough.py').read()); print('probe parses OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'subsystem_status|all_real' scripts/probe_live_duckduckgo_walkthrough.py` shows the all_real:true gate BEFORE any assertion
    - `grep -nE 'duckduckgo|scan' scripts/probe_live_duckduckgo_walkthrough.py` shows the real DuckDuckGo scan trigger + WS watch
    - The probe asserts at least one real inherited typed field (N.4) and at least one real chunk in the {chunk samples} iteration
    - `python -c "import ast; ast.parse(open('scripts/probe_live_duckduckgo_walkthrough.py').read())"` exits 0 (the real run is gated behind Task 4's checkpoint)
  </acceptance_criteria>
  <done>probe_live_duckduckgo_walkthrough.py exists, gates on all_real:true, triggers a real Selenium DuckDuckGo scan, asserts real inherited typed fields + real per-sample chunk iteration, and contains no stub fallback.</done>
</task>

<task type="auto">
  <name>Task 3: EXPLORE-04 e2e case (stub-backed §N walkthrough)</name>
  <read_first>
    - frontend_e2e/object_exploration.spec.js (the spec from Plans 03/05 — ADD the §N walkthrough case to it)
    - frontend_e2e/playwright.config.js (stub-boot fixture — the e2e runs stub-only; the real run is the probe in Task 2/4)
  </read_first>
  <action>
    Add a duckduckgo-walkthrough e2e case to frontend_e2e/object_exploration.spec.js that drives the §N sequence through the served editor against the STUB backend: author self=duckduckgo, drag-wire the (stub) WebBrowser scanner, assert the panel shows DuckDuckGo / {scanner} with NO types, right-click {scanner} to reveal type-stripped rank-1 url{}/dom{}, and advance the {chunk samples} iterator asserting per-sample focus changes. This is the fast DOM-level gate complementing the REPL scenario; the real-subsystem proof is Task 2's probe. Reuse the stub fixture; assert no stroke-dasharray anywhere (Forbidden Concepts guard).
  </action>
  <verify>
    <automated>cd frontend_e2e && npx playwright test object_exploration.spec.js -g "duckduckgo|walkthrough"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test object_exploration.spec.js -g "duckduckgo|walkthrough"` passes against the stub backend
    - The case asserts DuckDuckGo / {scanner} renders with NO colon-type slot (rank-1 minimalism)
    - The case asserts revealed url{}/dom{} are type-stripped and the {chunk samples} stepper advances
    - No stroke-dasharray anywhere (Forbidden Concepts guard)
  </acceptance_criteria>
  <done>object_exploration.spec.js carries a passing stub-backed §N walkthrough case complementing the REPL scenario.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Clean-GPU preflight + real-subsystem DuckDuckGo acceptance run (D-01)</name>
  <action>BLOCKING HUMAN CHECKPOINT — the human performs the clean-GPU preflight and the real-subsystem acceptance run from the MAIN context per D-01; the executor does not auto-run this (a wedged CUDA/Selenium boot hangs with no error). See how-to-verify for the exact steps.</action>
  <what-built>
    The duckduckgo-walkthrough REPL scenario (Task 1, green in stub) and probe_live_duckduckgo_walkthrough.py (Task 2). Before the real-subsystem acceptance run, D-01 mandates a clean-GPU preflight that the executor cannot safely self-verify (a wedged CUDA/Selenium state hangs the boot indefinitely with no error).
  </what-built>
  <how-to-verify>
    1. Confirm a clean GPU: no stray python or firefox processes (`tasklist | findstr /I "python firefox"` shows none from prior runs), and ~0 MiB VRAM in use (`nvidia-smi`). If any stray process exists, tear it down with `taskkill /F /T` before proceeding (per STATE.md blocker).
    2. From the MAIN context (NOT a verifier subagent — RESEARCH Pitfall 5), boot the real backend on :8080 with no fake gates set, and confirm `GET /api/subsystem_status` returns `all_real: true`.
    3. Run the real REPL scenario: `python -m scripts.sim_frontend env-scenario --name duckduckgo-walkthrough --backend http://127.0.0.1:8080` (no WFH_FAKE_* / NO_WEBDRIVER set) — expect green against the real Selenium scan.
    4. Run the live probe: `python scripts/probe_live_duckduckgo_walkthrough.py --backend http://127.0.0.1:8080` — expect it to assert all_real:true, a real DuckDuckGo scan with ≥1 real chunk, ≥1 real inherited typed field, and a real {chunk samples} iteration.
    5. Tear down the real backend with `taskkill /F /T` and confirm VRAM returns to ~0.
  </how-to-verify>
  <resume-signal>Type "approved" once the real REPL scenario AND the live probe both pass against all_real:true, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live web (DuckDuckGo)→scanner | untrusted real web content crosses into the real Selenium scan + materialiser |
| operator→real backend lifecycle | the human-gated clean-GPU preflight + real boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Denial of Service | wedged CUDA/Selenium on real boot | mitigate | Blocking-human checkpoint enforces the clean-GPU preflight + main-context lifecycle + taskkill teardown (D-01, RESEARCH Pitfall 5). |
| T-07-12 | Tampering | quiet stub fallback masking a failed real boot | mitigate | The probe asserts all_real:true FIRST and contains no stub fallback; a failed real subsystem surfaces as a loud failure, never a silent stub substitution (no-mocks contract). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- STUB fast gate: `env-scenario --name duckduckgo-walkthrough` green; `object_exploration.spec.js -g "duckduckgo"` green; `full-smoke` (stub) green.
- Probe + real scenario parse/import clean; the REAL run is gated behind the blocking-human clean-GPU checkpoint.
- ACCEPTANCE (post-checkpoint): real REPL scenario + probe_live_duckduckgo_walkthrough.py both green against all_real:true, with ≥1 real chunk + ≥1 real inherited typed field.
- No parallel log stream added; no stub fallback in the probe.
</verification>

<success_criteria>
- The §N DuckDuckGo walkthrough runs end-to-end: stub fast-gate (REPL scenario + e2e) green BOTH modes, and the real-subsystem probe + real REPL scenario pass against all_real:true after the clean-GPU preflight.
- The full §N sequence holds: author self=duckduckgo, drag-wire the scanner with inheritance, reveal type-stripped rank-1 url{}/dom{}, per-sample chunk iteration on real chunks.
</success_criteria>

<output>
Create `.planning/phases/WFH-07-deep-object-exploration-gestures/07-06-SUMMARY.md` when done
</output>
