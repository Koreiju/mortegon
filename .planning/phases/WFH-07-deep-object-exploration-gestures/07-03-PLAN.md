---
phase: 07-deep-object-exploration-gestures
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - backend/static/js/fe/magic_markdown.mjs
  - backend/static/js/fe/integration.test.mjs
  - frontend_e2e/object_exploration.spec.js
autonomous: true
requirements: [EXPLORE-02]
must_haves:
  truths:
    - "A {ref} renders in one of three states over ONE invariant graph link: braced-hidden, revealed-internal, resolved-external"
    - "Revealing a {ref} in panel form reveals it in graph form (node-count parity)"
    - "An external {ref} propagates as its own recursively-rendered rank-1 panel"
    - "A {ref} resolves to the LIVE current node at render time (duplicate-instance proxy semantics, N.6) — no frozen snapshot"
  artifacts:
    - path: "backend/static/js/fe/magic_markdown.mjs"
      provides: "brace-state classification (braced-hidden/revealed-internal/resolved-external) over renderPanel/renderGraph Line[]/GraphNode"
      contains: "resolved"
    - path: "frontend_e2e/object_exploration.spec.js"
      provides: "e2e: ref-propagation render + brace-state transitions"
      contains: "ref"
  key_links:
    - from: "magic_markdown.mjs brace-state selection"
      to: "buildRegistry/refTarget live resolution"
      via: "a {ref} is re-resolved against the live registry at render time, not snapshotted (N.6 proxy)"
      pattern: "refTarget|buildRegistry"
---

<objective>
Implement the three §O.1a brace render states (D-04, pulled into Phase 7) over the one invariant graph link, and verify external-`{ref}` recursive propagation + duplicate-instance proxy semantics (EXPLORE-02). Per RESEARCH Open Q1, FIRST test whether the existing `buildRegistry`/`refTarget` live-resolution already satisfies N.6's "operationally calls the originating object" before building any new proxy state — this may be a zero-new-backend item.

Purpose: A `{ref}` is internal/external + folded/unfolded as RENDERING choices over one ConceptEdge, never three edge types (D-04). The fold-expand loop is already proven at the model level by `integration.test.mjs`; this plan adds the brace-state classification on top and proves duplicate-instance proxy + node-count parity.
Output: brace-state render classification + duplicate-instance test + the new e2e spec (ref-propagation + brace transitions).
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-PATTERNS.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-UI-SPEC.md
@backend/static/js/fe/magic_markdown.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brace-state classification + N.6 proxy verification (Open Q1 first)</name>
  <read_first>
    - backend/static/js/fe/magic_markdown.mjs lines 121-200 (buildRegistry, refTarget, refTargets, renderPanel — the live-resolution mechanism + Line shape; confirm refTarget resolves to the CURRENT live node each render, not a snapshot)
    - backend/static/js/fe/integration.test.mjs (the passing right-click-{ref}→fold→expand-inline loop — the EXPLORE-02 propagation pattern already proven at the model level)
    - .planning/phases/WFH-07-deep-object-exploration-gestures/07-UI-SPEC.md "Three Brace Render States" table (braced-hidden / revealed-internal / resolved-external triggers + the panel↔graph parity + NO dashed lines constraint)
    - docs/frontend/object_exploration.md §N.6/§N.7/§N.14 (duplicate-instance "operationally calls the originating object" — read closely per RESEARCH Open Q1 before building anything)
  </read_first>
  <behavior>
    - Test 1 (Open Q1 probe): a {ref} whose target node's text is then mutated re-resolves to the NEW text on the next renderPanel call (live proxy, not frozen snapshot) — proving the existing buildRegistry/refTarget already gives N.6 for free.
    - Test 2: an unrevealed {ref} to a node NOT otherwise visible classifies as braced-hidden (braces retained, glyph ▸, no children lines).
    - Test 3: after toggle(expanded, refLine.path), the same {ref} classifies as revealed-internal (braces dropped, rank-1 child lines present, source:"expanded").
    - Test 4: a {ref} whose target is ALSO independently visible (revealed via another path) classifies as resolved-external (a solid-link marker, NOT inline children, NOT dashed).
    - Test 5: node-count parity — revealing a ref in the panel Line[] yields the same revealed node in renderGraph's GraphNode set.
  </behavior>
  <action>
    Add a pure brace-state classifier to backend/static/js/fe/magic_markdown.mjs that, for each {ref} line, returns one of three states based on backend-supplied facts (is the target node visible elsewhere? is this ref folded/expanded?) — exactly the D10 split the RESEARCH Responsibility Map specifies (rendering decision over fold-state + visibility, no new backend logic). braced-hidden = ref unrevealed AND target not otherwise visible; revealed-internal = ref expanded (existing fold path, braces drop, children indent — reuse the existing renderPanel expansion, do not duplicate it); resolved-external = target node independently visible (emit a solid-link marker on the Line, no inline children). Expose the state on each ref Line (e.g. a `braceState` field) so panelVDom/graphVDom can pick the render. CRITICAL: this is a classification over the EXISTING renderPanel/renderGraph output — one ConceptEdge, three render states; never create three edge records. For N.6: do NOT build new proxy state UNTIL Test 1 proves whether the existing refTarget live-resolution already satisfies "operationally calls the originating object" — if Test 1 passes against the unmodified mechanism, record in the SUMMARY that N.6 is satisfied by live-registry-resolution (zero new code) per Open Q1; only if it fails do you add proxy state, and document why.
  </action>
  <verify>
    <automated>node backend/static/js/fe/integration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node backend/static/js/fe/integration.test.mjs` exits 0 with the five new cases passing
    - `grep -nE 'braceState|resolved|braced' backend/static/js/fe/magic_markdown.mjs` shows the three-state classifier
    - The SUMMARY records the Open-Q1 finding: whether N.6 proxy is satisfied by existing live-resolution (preferred) or required new state
    - No dashed/dotted line is introduced (resolved-external is a SOLID-link marker only)
  </acceptance_criteria>
  <done>The three brace render states classify correctly over one invariant graph link, revealing a ref shows panel↔graph node-count parity, the N.6 proxy question is resolved (live-resolution preferred), and the integration tests prove all five behaviors with no dashed lines.</done>
</task>

<task type="auto">
  <name>Task 2: e2e — ref-propagation render + brace-state transitions</name>
  <read_first>
    - frontend_e2e/projector.spec.js (Phase 6 spec — the Playwright structure + how it drives the served editor.html and inspects DOM)
    - frontend_e2e/black_slate.spec.js (the no-dasharray invariant the brace-state e2e must NOT break — resolved-external must stay solid)
    - frontend_e2e/playwright.config.js (the stub-boot fixture: WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1, reuses :8080)
  </read_first>
  <action>
    Create frontend_e2e/object_exploration.spec.js (the net-new shared e2e spec for this phase — later plans add cases to the SAME file). Add the EXPLORE-02 cases: (1) author a card with an external {ref}, render the panel in the served editor, assert the {ref} renders braced-hidden (braces present, ▸ glyph) by reading computed DOM; (2) toggle the fold and assert the rank-1 child lines appear inline (revealed-internal, braces dropped); (3) make the target independently visible and assert the resolved-external state draws a SOLID link (no stroke-dasharray) — reuse the black_slate.spec.js dasharray assertion idiom so this case doubles as a no-dotted-lines guard. Use the existing playwright.config.js stub fixture; do NOT boot real subsystems for this spec.
  </action>
  <verify>
    <automated>cd frontend_e2e && npx playwright test object_exploration.spec.js -g "ref" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test object_exploration.spec.js -g "ref"` passes from frontend_e2e/
    - The resolved-external case asserts NO stroke-dasharray on the link (solid-only, reusing the black_slate idiom)
    - `npx playwright test black_slate.spec.js` still passes (no dotted-line regression)
  </acceptance_criteria>
  <done>object_exploration.spec.js exists with passing ref-propagation + brace-state e2e cases; the resolved-external link is solid; black_slate.spec.js stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ref→registry | a {ref} token resolves against the live registry at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Denial of Service | recursive {ref} (self/cycle) | mitigate | Rank-1 minimalism bounds reveal depth; buildRegistry/refTarget resolve by LOOKUP not by walking, inherently cycle-safe (RESEARCH Security Domain). The classifier must not introduce deep auto-recursion. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node backend/static/js/fe/integration.test.mjs` exits 0 (five brace-state + proxy cases).
- `npx playwright test object_exploration.spec.js -g "ref"` passes; `black_slate.spec.js` stays green (no dasharray).
- N.6 proxy resolution recorded in SUMMARY (live-resolution preferred per Open Q1).
- One ConceptEdge, three render states — never three edge records.
</verification>

<success_criteria>
- The three brace render states classify and render correctly over one invariant graph link with panel↔graph node-count parity.
- External {ref} propagation + duplicate-instance proxy verified; resolved-external draws a solid (never dashed) link.
</success_criteria>

<output>
Create `.planning/phases/WFH-07-deep-object-exploration-gestures/07-03-SUMMARY.md` when done
</output>
