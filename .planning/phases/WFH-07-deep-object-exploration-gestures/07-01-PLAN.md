---
phase: 07-deep-object-exploration-gestures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/routes.py
  - backend/tests/test_next_rank_route.py
autonomous: true
requirements: [EXPLORE-01]
must_haves:
  truths:
    - "GET /api/concepts/{id}/next_rank returns the rank-1 typed neighbors of a python-native node"
    - "The route filters ConceptEdge rows by the four materialiser edge types only, never a parallel vocabulary"
    - "The route reads+shapes graph data backend-side; no type computation is exposed for the frontend to do (D10)"
  artifacts:
    - path: "backend/api/routes.py"
      provides: "GET /concepts/{id}/next_rank rank-1 type-graph fetch endpoint (D-03)"
      contains: "next_rank"
    - path: "backend/tests/test_next_rank_route.py"
      provides: "pytest coverage for the new endpoint over a materialised python_object tree"
      contains: "next_rank"
  key_links:
    - from: "backend/api/routes.py (next_rank)"
      to: "backend/services/python_api_materialiser.py edge vocabulary"
      via: "list_concept_edges filtered to OBJECT_HAS_PROPERTY/OBJECT_HAS_FUNCTION/FUNCTION_INPUT_TYPE/FUNCTION_OUTPUT_TYPE"
      pattern: "OBJECT_HAS_PROPERTY|FUNCTION_INPUT_TYPE"
---

<objective>
Add the backend rank-1 type-graph fetch endpoint (D-03) that EXPLORE-01's hover/right-click next-rank expansion renders over. The frontend renders the type graph but never computes it (D10) — this route does the ConceptEdge traversal server-side and returns a shaped rank-1 typed-neighbor list.

Purpose: EXPLORE-01 needs to fetch the super-class + typed constructor params of a node, and a function row's typed I/O, without the frontend walking the graph. RESEARCH Open Q2 + Assumption A1 settle on a new dedicated GET route (additive, self-documenting) over extending GET /concepts/{id}.
Output: GET /api/concepts/{id}/next_rank + its pytest coverage.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /concepts/{id}/next_rank rank-1 type-graph endpoint (D-03)</name>
  <read_first>
    - backend/api/routes.py lines 2287-2405 (the static-path-before-parametric block + the route-ordering NOTE at line 2290; /concepts/export and /import show where static paths must live)
    - backend/api/routes.py lines 5043-5149 (EditorLinkRequest + editor_link — the Pydantic/router/_get_graph_editor conventions to mirror)
    - backend/services/python_api_materialiser.py lines 19-22, 540-600 (the four edge-type constants the route must read: OBJECT_HAS_PROPERTY, OBJECT_HAS_FUNCTION, FUNCTION_INPUT_TYPE, FUNCTION_OUTPUT_TYPE)
  </read_first>
  <action>
    Add a GET route /concepts/{concept_id}/next_rank to backend/api/routes.py. CRITICAL placement: register it in the static-path block ABOVE the parametric /concepts/{concept_id} route (line 2399) — same hazard the line-2290 NOTE describes — so next_rank is not swallowed as a literal concept id. Use _get_graph_editor() as the data-access seam (same as editor_link). Implementation: fetch the node via ge.get_concept(concept_id) (404 via HTTPException if None, mirroring get_concept_by_id at 2399-2405); call ge.list_concept_edges(workspace_id=workspace_id, limit=50000) and filter to rows whose source_id equals concept_id AND whose edge_type is in the set of the four materialiser edge-type strings; resolve each edge's target_id to a ConceptNode via ge.get_concept; return a shaped list of rank-1 neighbors, each carrying concept_id, name, type_hint, edge_type, and a relation render-hint mapping the edge_type to property / function / input_type / output_type. Accept workspace_id as a query param defaulting to empty string (same convention as export_concept_graph). Guard against self-referential edges (skip any edge whose target_id equals concept_id) per the RESEARCH Security Domain DoS note — rank-1 only, never walk beyond. Do NOT compute or infer types in this route beyond reading the materialiser's already-written edges (D10 / D-03: new endpoints OK, new type COMPUTATION stays in the materialiser). Use query params only (no Pydantic body for a GET) but keep the docstring convention of the surrounding routes.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/api/routes.py').read()); print('routes.py parses OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'next_rank' backend/api/routes.py` shows the route registered BEFORE the `@router.get("/concepts/{concept_id}")` line (static-path-first ordering)
    - `grep -nE 'OBJECT_HAS_PROPERTY|OBJECT_HAS_FUNCTION|FUNCTION_INPUT_TYPE|FUNCTION_OUTPUT_TYPE' backend/api/routes.py` shows all four edge-type strings referenced inside the new handler (the SAME vocabulary as python_api_materialiser.py)
    - `python -c "import ast; ast.parse(open('backend/api/routes.py').read())"` exits 0
  </acceptance_criteria>
  <done>GET /concepts/{id}/next_rank exists, is ordered before the parametric route, filters by exactly the four materialiser edge types, resolves targets to ConceptNodes, skips self-referential edges, and returns a shaped rank-1 neighbor list. No type computation added frontend-side.</done>
</task>

<task type="auto">
  <name>Task 2: Pytest coverage for next_rank over a materialised python_object tree</name>
  <read_first>
    - scripts/probe_python_api.py (assertion style for python-native materialised trees + OBJECT_HAS_*/FUNCTION_*_TYPE transitive-closure proof — the existing precedent this test mirrors)
    - backend/services/python_api_materialiser.py lines 540-600 (how the four edge types are written, so the test can seed/assert against a real materialised tree)
    - backend/api/routes.py (the new next_rank route from Task 1 — its exact response shape)
  </read_first>
  <action>
    Create backend/tests/test_next_rank_route.py. Use FastAPI's TestClient against the app (or call the route handler directly with a graph editor seeded via the materialiser), under WFH_FAKE_SLM=1 / WFH_FAKE_EMBEDDER=1 / NO_WEBDRIVER=1 (no real subsystems needed for a pure graph-read test; use backend/services/db_janitor.py temp_db_dir to avoid leaking test DBs per CLAUDE.md §R.9). Seed a python-native node whose rank-1 neighbors are known (reuse the materialiser path probe_python_api.py exercises, or a minimal fixture object), call GET /concepts/{id}/next_rank, and assert: (1) every returned neighbor's edge_type is one of the four materialiser types; (2) a known property and a known function neighbor both appear; (3) a self-referential edge (if seeded) is excluded; (4) a non-existent concept_id returns 404. Confirm the project's pytest invocation during execution (RESEARCH flagged no pytest.ini / [tool.pytest] found — check for a conftest.py and how prior backend tests run; if no pytest harness exists, write the test as a runnable python -m script following probe_python_api.py's standalone-assert style and record that choice in the SUMMARY).
  </action>
  <verify>
    <automated>WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python -m pytest backend/tests/test_next_rank_route.py -x 2>&1 | tail -20 || WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python backend/tests/test_next_rank_route.py</automated>
  </verify>
  <acceptance_criteria>
    - The test command exits 0 with all assertions passing (pytest green, or standalone-script prints OK and returns 0)
    - The test asserts every returned neighbor edge_type is one of the four materialiser types
    - The test asserts a 404 for an unknown concept_id
  </acceptance_criteria>
  <done>backend/tests/test_next_rank_route.py exists and passes against a materialised python_object tree under the fake-subsystem gates, proving the route returns rank-1 typed neighbors with only the four materialiser edge types and 404s on unknown ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (GET /next_rank) | concept_id + workspace_id query params cross from the browser into a graph read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | next_rank graph traversal | mitigate | Rank-1 ONLY — skip self-referential edges, never recurse beyond direct neighbors; the existing list_concept_edges limit bounds the read (RESEARCH Security Domain). |
| T-07-02 | Tampering | unknown/cross-workspace concept_id | mitigate | get_concept returns None → HTTPException(404); workspace_id scopes list_concept_edges, same as existing concept routes. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — pure in-repo extension). |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('backend/api/routes.py').read())"` exits 0.
- `WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python -m pytest backend/tests/test_next_rank_route.py -x` (or the standalone-script fallback) passes.
- D10/D-03 boundary held: the route reads materialiser edges; no type computation moved frontend-side.
</verification>

<success_criteria>
- GET /api/concepts/{id}/next_rank returns rank-1 typed neighbors filtered to the four materialiser edge types, ordered before the parametric concepts route, with 404 on unknown ids and self-referential edges skipped.
- A passing automated test proves the behavior under the fake-subsystem gates.
</success_criteria>

<output>
Create `.planning/phases/WFH-07-deep-object-exploration-gestures/07-01-SUMMARY.md` when done
</output>
