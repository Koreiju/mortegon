---
phase: 07-deep-object-exploration-gestures
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/api/routes.py
  - backend/static/js/fe/gateway.mjs
  - backend/static/js/fe/gateway.test.mjs
  - backend/tests/test_edge_inherit_types.py
autonomous: true
requirements: [EXPLORE-03]
must_haves:
  truths:
    - "Creating an edge with inherit_types copies the source node's I/O types + object model onto the target (N.4)"
    - "Double-right-delete of a wired {ref} also removes the backing ConceptEdge, not just the value (N.13)"
    - "The inheritance side-effect is one synchronous request through the existing lifecycle dispatcher (cascade-is-default)"
  artifacts:
    - path: "backend/api/routes.py"
      provides: "edge-create I/O-type-inheritance extension (inherit_types) on the editor_link / concept_edges path"
      contains: "inherit_types"
    - path: "backend/static/js/fe/gateway.mjs"
      provides: "WIRE_LINK body carries inherit_types; DELETE_REF also fires DELETE /concept_edges/{edge_id} when a backing edge exists"
      contains: "inherit_types"
    - path: "backend/tests/test_edge_inherit_types.py"
      provides: "pytest proving the target gains the source's typed fields after a wire with inherit_types"
      contains: "inherit"
  key_links:
    - from: "routes.py edge-create inherit_types"
      to: "apply_edge_create_lifecycle"
      via: "type-copy runs as a synchronous side-effect inside the same handler, fanned out through the existing lifecycle dispatcher"
      pattern: "apply_edge_create_lifecycle"
---

<objective>
Build the backend + HTTP seam for EXPLORE-03's drag-wire type-inheritance (N.4) and complete double-right-delete (N.13). RESEARCH confirms NO legacy precedent exists for N.4 (`cp/edge_manager.js` is a pure THREE.js geometry mixin) — this is GREENFIELD against `object_exploration.md` N.4, using `EditorLinkRequest`/`editor_link` as a STRUCTURAL template only.

Purpose: Today the edge-create path (`editor_link` / `POST /concept_edges`) creates a plain edge with NO type-inheritance side effect (RESEARCH Pitfall 2; `EditorLinkRequest` has only source_id/target_id/edge_type/workspace_id/idempotency_key). And `DELETE_REF` maps only to value-clear, leaving the backing edge alive (RESEARCH Pitfall 1 / Assumption A2). This plan adds the `inherit_types` side-effect and the edge-delete completion as the substrate the DOM drag-wire (Plan 05) calls.
Output: `inherit_types` edge-create extension + gateway WIRE_LINK/DELETE_REF extensions + backend test + gateway unit test.

Wave note: this plan depends only on 07-01 (Wave 1) and is placed in Wave 2. It shares NO files_modified with the other Wave-2 plan 07-03 (07-04 = routes.py + gateway.mjs/gateway.test.mjs + test_edge_inherit_types.py; 07-03 = magic_markdown.mjs + integration.test.mjs + object_exploration.spec.js), so the two run in parallel safely; placing 07-04 in Wave 2 (not Wave 3) shortens the critical path to 07-05/07-06.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-PATTERNS.md
@backend/static/js/fe/gateway.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: edge-create I/O-type-inheritance extension (N.4, GREENFIELD)</name>
  <read_first>
    - backend/api/routes.py lines 5043-5149 (EditorLinkRequest field list + editor_link handler + the apply_edge_create_lifecycle call at ~5139-5142 — the STRUCTURAL template; confirmed NO type/ports/inherit field today)
    - backend/api/routes.py lines 5386-5430 (POST /concept_edges + DELETE /concept_edges/{edge_id} — the edge-create + edge-delete primitives)
    - backend/services/python_api_materialiser.py lines 540-600 (the four edge types whose targets the inheritance step copies: OBJECT_HAS_PROPERTY/OBJECT_HAS_FUNCTION/FUNCTION_INPUT_TYPE/FUNCTION_OUTPUT_TYPE)
    - docs/frontend/object_exploration.md N.4 (the "target inherits source's I/O types + object model" spec — GREENFIELD, no legacy port)
  </read_first>
  <behavior>
    - Test 1: editor_link / concept_edges WITHOUT inherit_types behaves exactly as today (plain edge, no type copy) — backward compatible.
    - Test 2: editor_link / concept_edges WITH inherit_types=true, after the edge is created, the target node gains equivalent typed fields/edges read from the source's ports / OBJECT_HAS_*/FUNCTION_*_TYPE edges.
    - Test 3: an invalid source/target pair still raises HTTPException(400) (validation preserved, not bypassed for a "fast path" — RESEARCH Security Domain Tampering).
    - Test 4: the inheritance runs through apply_edge_create_lifecycle so WS/index/evolution-log fan-out fires once (one synchronous side-effect, cascade-is-default).
  </behavior>
  <action>
    Add an optional `inherit_types: bool = False` field to EditorLinkRequest (and accept the same flag on the POST /concept_edges request model if it has its own). When inherit_types is true, AFTER `ge.create_concept_edge(...)` succeeds and BEFORE/within the existing apply_edge_create_lifecycle call, read the source node's I/O types + object model — its `ports` (inputs/outputs) and/or its OBJECT_HAS_PROPERTY/OBJECT_HAS_FUNCTION/FUNCTION_INPUT_TYPE/FUNCTION_OUTPUT_TYPE edges via list_concept_edges filtered by source_id — and write equivalent typed edges/fields onto the target node. This is a graph mutation with derived data (D10 Responsibility Map: belongs server-side). Run it as a SINGLE synchronous side-effect inside the SAME handler (RESEARCH Open Q3: prefer one request, one lifecycle event, consistent with cascade-is-default) — do NOT make the frontend orchestrate a two-step sequence. Preserve the existing try/except → HTTPException(400) on invalid edges (do not bypass validation). Keep the default-False path byte-for-byte behavior-identical to today so existing callers are unaffected. Use the SAME four materialiser edge-type strings — never a parallel vocabulary.
  </action>
  <verify>
    <automated>WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python -m pytest backend/tests/test_edge_inherit_types.py -x 2>&1 | tail -20 || WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python backend/tests/test_edge_inherit_types.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'inherit_types' backend/api/routes.py` shows the new field on the request model AND the conditional side-effect in the handler
    - `grep -n 'apply_edge_create_lifecycle' backend/api/routes.py` confirms the inheritance fans out through the existing dispatcher (not a parallel write path)
    - The backend test (Task 3 file) proves the target gains the source's typed fields with inherit_types=true and is unchanged with it false
  </acceptance_criteria>
  <done>edge-create accepts inherit_types; when true the target inherits the source's I/O types + object model via the four materialiser edge types as one synchronous lifecycle-dispatched side-effect; the default-false path is unchanged; invalid edges still 400.</done>
</task>

<task type="auto">
  <name>Task 2: gateway WIRE_LINK inherit_types + DELETE_REF edge-delete completion (N.13)</name>
  <read_first>
    - backend/static/js/fe/gateway.mjs lines 54-63 (the WIRE_LINK and DELETE_REF cases — the exact switch arms to extend) + lines 81-99 (GestureGateway.send — the two-step wrapper to use if DELETE_REF needs a second call)
    - backend/api/routes.py lines 5414-5430 (DELETE /concept_edges/{edge_id} — the existing route the DELETE_REF completion fires)
    - .planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md Pitfall 1 + Assumption A2 (DELETE_REF should ALSO fire DELETE /concept_edges/{edge_id} when a backing edge exists — confirm against object_exploration.md §N.13 wording during execution before committing the destructive path)
  </read_first>
  <action>
    Extend backend/static/js/fe/gateway.mjs buildRequest: (a) the WIRE_LINK / concept-edge-create case adds `inherit_types: !!g.inheritTypes` (default false so non-wire callers are unaffected) to the POST /api/concept_edges body — single synchronous side-effect per Open Q3. (b) DELETE_REF (N.13): per RESEARCH Pitfall 1 / Assumption A2, when the deleted {ref} corresponds to a backing ConceptEdge (g.edgeId present), the delete must remove the EDGE, not just clear the value. Implement this through the existing GestureGateway.send wrapper firing the edit_close call AND a DELETE /api/concept_edges/{edge_id} call (use the send wrapper's responsibility for sequential buildRequest-shaped calls — do NOT inline a raw fetch() elsewhere). When g.edgeId is absent (a plain literal {ref} with no backing edge) keep the existing value-clear-only behavior. Decide and DOCUMENT this explicitly in the SUMMARY (confirm against object_exploration.md §N.13 that deleting the reference is meant to delete the underlying edge, not only the text) rather than leaving a silent partial delete.
  </action>
  <verify>
    <automated>node backend/static/js/fe/gateway.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'inherit_types' backend/static/js/fe/gateway.mjs` shows the flag on the WIRE_LINK body
    - `grep -nE 'concept_edges/|edgeId' backend/static/js/fe/gateway.mjs` shows DELETE_REF firing an edge delete when a backing edge exists
    - `node backend/static/js/fe/gateway.test.mjs` passes (new WIRE_LINK + DELETE_REF cases)
    - The SUMMARY records the N.13 decision (edge-delete vs value-clear-only) confirmed against object_exploration.md §N.13
  </acceptance_criteria>
  <done>gateway WIRE_LINK carries inherit_types; DELETE_REF removes the backing edge (via DELETE /concept_edges/{edge_id}) when one exists and falls back to value-clear when not; the choice is documented; gateway unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: pytest for edge-create inheritance + gateway unit-test scaffold</name>
  <read_first>
    - scripts/probe_python_api.py (assertion style for materialised typed trees — to seed a source with known I/O types)
    - backend/static/js/fe/gateway.test.mjs IF it exists (extend it); otherwise model from backend/static/js/fe/magic_markdown_gestures.test.mjs assertion style (node:assert + test())
    - backend/services/db_janitor.py (temp_db_dir ctx-manager — use it so the backend test leaks no DB, per CLAUDE.md §R.9)
  </read_first>
  <action>
    Create backend/tests/test_edge_inherit_types.py implementing Task 1's four behavior cases under WFH_FAKE_SLM=1/WFH_FAKE_EMBEDDER=1/NO_WEBDRIVER=1 with a db_janitor temp DB: seed a source python-native node with known I/O types, create an edge with inherit_types=true, assert the target gains equivalent typed edges/fields; assert the inherit_types=false path leaves the target untyped; assert an invalid pair 400s. If backend/static/js/fe/gateway.test.mjs does not exist yet, create it with the WIRE_LINK-inherit_types and DELETE_REF-edge-delete buildRequest cases (assert the returned {method,path,body} shape); if it exists, extend it. Follow the project's pytest invocation convention confirmed in Plan 01.
  </action>
  <verify>
    <automated>node backend/static/js/fe/gateway.test.mjs && WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python -m pytest backend/tests/test_edge_inherit_types.py -x 2>&1 | tail -20 || WFH_FAKE_SLM=1 WFH_FAKE_EMBEDDER=1 NO_WEBDRIVER=1 python backend/tests/test_edge_inherit_types.py</automated>
  </verify>
  <acceptance_criteria>
    - `node backend/static/js/fe/gateway.test.mjs` exits 0 with the WIRE_LINK-inherit_types + DELETE_REF-edge-delete cases
    - The backend inheritance test exits 0 (target gains typed fields with inherit_types=true; untouched with false; invalid pair 400s)
  </acceptance_criteria>
  <done>Both the gateway unit cases and the backend inheritance pytest pass, proving the inherit_types side-effect and the DELETE_REF edge-delete request shaping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (edge create/delete) | source_id/target_id/edge_id cross from the browser into graph mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | drag-wire edge across workspace / invalid pair | mitigate | Existing create_concept_edge raises ValueError→HTTPException(400); the inherit_types extension preserves this validation, never a fast-path bypass (RESEARCH Security Domain). |
| T-07-07 | Repudiation | inheritance side-effect not logged | mitigate | Inheritance runs through apply_edge_create_lifecycle → evolution-log fan-out, so the mutation is auditable (append-only log invariant). |
| T-07-08 | Tampering | DELETE_REF over-reach deleting wrong edge | mitigate | Edge-delete fires ONLY when g.edgeId names a real backing edge; decision confirmed against §N.13 and documented (Assumption A2). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node backend/static/js/fe/gateway.test.mjs` exits 0.
- `python -m pytest backend/tests/test_edge_inherit_types.py -x` (or standalone fallback) passes.
- inherit_types fans out through apply_edge_create_lifecycle; default-false path unchanged; DELETE_REF edge-delete decision documented vs §N.13.
- D10/D-03 boundary: inheritance is a backend graph mutation; the frontend only sends the flag.
</verification>

<success_criteria>
- Drag-wire with inherit_types copies the source's I/O types + object model onto the target as one synchronous lifecycle-dispatched side-effect.
- Double-right-delete removes the backing ConceptEdge (N.13) when one exists; both behaviors are proven by automated tests.
</success_criteria>

<output>
Create `.planning/phases/WFH-07-deep-object-exploration-gestures/07-04-SUMMARY.md` when done
</output>
