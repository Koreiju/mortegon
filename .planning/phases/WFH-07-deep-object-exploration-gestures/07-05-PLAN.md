---
phase: 07-deep-object-exploration-gestures
plan: 05
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - backend/static/js/fe/magic_markdown_panel.mjs
  - backend/static/js/fe/magic_markdown_panel.test.mjs
  - frontend_e2e/object_exploration.spec.js
autonomous: true
requirements: [EXPLORE-03, EXPLORE-01]
must_haves:
  truths:
    - "mount() captures contextmenu (right-click fold), double-right-click (delete), and mousedown/mousemove/mouseup (drag-wire)"
    - "Every new handler calls the existing resolveGesture(), never an inline classification chain"
    - "Right-click fold preserves per-subtree fold state across collapse/re-expand (M.6)"
    - "Read-only python-native nodes refuse single-left edit but allow hover/right-click/double-left (🔒)"
    - "Hover on a typed field previews the next-rank type graph (EXPLORE-01 render over Plan 01's endpoint)"
  artifacts:
    - path: "backend/static/js/fe/magic_markdown_panel.mjs"
      provides: "mount() DOM handlers: contextmenu, double-right debounce, drag state machine, 🔒 single-left gate, hover next-rank preview"
      contains: "contextmenu"
    - path: "backend/static/js/fe/magic_markdown_panel.test.mjs"
      provides: "DOM-capture unit cases for the new handlers"
      contains: "contextmenu"
    - path: "frontend_e2e/object_exploration.spec.js"
      provides: "e2e: hover-type-graph, drag-wire, double-right-delete, fold-preservation"
      contains: "drag"
  key_links:
    - from: "magic_markdown_panel.mjs mount() handlers"
      to: "magic_markdown_gestures.mjs resolveGesture"
      via: "every handler populates {button,clicks,target,mode,drag} and calls resolveGesture — single source of truth"
      pattern: "resolveGesture"
    - from: "mount() WIRE_LINK / DELETE_REF / TOGGLE_FOLD"
      to: "gateway.mjs buildRequest / GestureGateway.send"
      via: "resolved actions route through the gateway (inherit_types on wire, edge-delete on double-right)"
      pattern: "buildRequest|send"
---

<objective>
Wire the missing DOM event capture in `magic_markdown_panel.mjs::mount()` so right-click-fold, double-right-delete, and drag-wire gestures reach the already-correct `resolveGesture()` in a real browser — plus the EXPLORE-01 hover next-rank preview that renders over Plan 01's endpoint. RESEARCH's central finding: the gesture resolver and request mapper are ALREADY correct; the only gap is that `mount()` wires solely `click` + `dblclick` (left-only).

Purpose: Gestures #3 (right-click fold), #6 (drag-wire), and #7 (double-right delete) of the seven-gesture model never fire today because `mount()` has no `contextmenu`, no double-right debounce, and no drag state machine. RESEARCH Pitfall 4: there is NO native double-right DOM event — it needs manual timestamp debounce on consecutive `contextmenu` events. The 🔒 read-only gate must refuse single-left edit while allowing exploration. The drag-wire path sets D-03's inherit_types flag (Plan 04) and the hover preview renders D-03's next-rank endpoint (Plan 01); all gestures route through the existing resolver per D-02 (extend existing fe/ modules, no new top-level module).
Output: the new `mount()` handlers + DOM-capture unit cases + the drag-wire/double-right/hover-type-graph/fold e2e cases.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-PATTERNS.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-UI-SPEC.md
@backend/static/js/fe/magic_markdown_panel.mjs
@backend/static/js/fe/magic_markdown_gestures.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mount() contextmenu + double-right debounce + drag state machine + 🔒 gate</name>
  <read_first>
    - backend/static/js/fe/magic_markdown_panel.mjs lines 146-173 (the existing mount() — the ev.target.closest(...) dispatch shape + handlers.onX convention to mirror exactly)
    - backend/static/js/fe/magic_markdown_gestures.mjs lines 1-77 (resolveGesture + the Action enum — the single classification function every new handler MUST call; note the {button,clicks,target,mode,drag} input shape)
    - backend/static/js/cp/concept_graph.js lines ~1366-1393 and ~1684-1708 (the legacy contextmenu node_fold handler + the read-only 🔒 gate /^fixture::/.test(backing_pointer) || /^python_/.test(type_hint) — the missing-gesture reference + read-only rule to carry over)
    - .planning/phases/WFH-07-deep-object-exploration-gestures/07-UI-SPEC.md "Interaction Contract" (the seven-gesture disambiguation table) + "Drag-Wire Affordance Contract" (transient SOLID line during drag, discard on empty-canvas release) + "Hover-Preview Contract"
  </read_first>
  <behavior>
    - Test 1: a contextmenu event on a ref/dropdown/token target calls resolveGesture({button:"right",clicks:1,target,mode}) → TOGGLE_FOLD, and the handler invokes handlers.onToggle (or the fold handler) with the structural data-path.
    - Test 2: a contextmenu event on the base/self node → COLLAPSE_TO_NODE.
    - Test 3: two contextmenu events on the SAME target within the debounce window synthesize {button:"right",clicks:2,target} → DELETE_REF (double-right delete; no native double-right event — manual debounce per Pitfall 4).
    - Test 4: mousedown on a graph node → mousemove past the move-threshold → mouseup over another node synthesizes {button:"left",drag:true} → WIRE_LINK; a press→release with no move does NOT classify as drag (stays a click/edit).
    - Test 5: mouseup over empty canvas (no target node) discards the drag — no WIRE_LINK fired, no error.
    - Test 6: on a read-only node (type_hint python_ or backing fixture::), single-left is a no-op (no onEdit) while contextmenu/dblclick still fire (🔒 gate).
  </behavior>
  <action>
    Extend backend/static/js/fe/magic_markdown_panel.mjs::mount() with new listeners that mirror the EXISTING ev.target.closest(...) dispatch shape and the handlers.onX && handlers.onX(...) convention. (1) contextmenu listener: ev.preventDefault(); classify target (self vs ref/dropdown/token via closest on .mm-gnode self vs .mm-drop/.mm-text); call resolveGesture({button:"right",clicks:1,target,mode:opts.mode}); route TOGGLE_FOLD/COLLAPSE_TO_NODE to the appropriate handler with the structural data-path (use the EXISTING fe/ path scheme, never legacy token-text — RESEARCH State of the Art). (2) double-right debounce: track {ts,target} on each contextmenu; if a second fires on the same target within ~400ms (implementation-discretion threshold per Assumption A3), call resolveGesture({button:"right",clicks:2,target}) → DELETE_REF instead of the single-right fold, and route through the gateway's DELETE_REF path (Plan 04's edge-delete completion — pass g.edgeId when the ref has a backing edge). (3) drag state machine (graph mode only): mousedown on a .mm-gnode records the source; mousemove past a few-pixel move-threshold flips into drag mode and renders a TRANSIENT SOLID line (no dash) from source to pointer per the Drag-Wire Affordance Contract; mouseup over another .mm-gnode calls resolveGesture({button:"left",drag:true}) → WIRE_LINK (pass inheritTypes:true so the gateway sets inherit_types per N.4); mouseup over empty canvas discards (remove the transient line, no edge, no error). (4) 🔒 gate: in the existing click handler, before onEdit, check the read-only condition (/^python_/.test(rootNode.type_hint||'') || /^fixture::/.test(rootNode.backing_pointer||'')) and make single-left a no-op highlight on read-only nodes while leaving contextmenu/dblclick paths intact. ALWAYS call resolveGesture — never inline a second classification chain (RESEARCH Don't Hand-Roll). The transient drag line must be SOLID (no stroke-dasharray) per the Forbidden Concepts list.
  </action>
  <verify>
    <automated>node backend/static/js/fe/magic_markdown_panel.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "addEventListener\\(['\"]contextmenu" backend/static/js/fe/magic_markdown_panel.mjs` shows the contextmenu listener wired in mount()
    - `grep -nE "mousedown|mousemove|mouseup" backend/static/js/fe/magic_markdown_panel.mjs` shows the drag state machine
    - `grep -n 'resolveGesture' backend/static/js/fe/magic_markdown_panel.mjs` shows every new handler calls the shared resolver (no inline if(button===) chain)
    - `node backend/static/js/fe/magic_markdown_panel.test.mjs` passes the six DOM-capture cases
  </acceptance_criteria>
  <done>mount() captures contextmenu, double-right (manual debounce), and drag (mousedown/move/up), all routed through resolveGesture; the 🔒 gate refuses single-left edit on read-only nodes while allowing exploration; the transient drag line is solid; the unit tests prove all six behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: DOM-capture unit-test scaffold (contextmenu, double-right debounce, drag)</name>
  <read_first>
    - backend/static/js/fe/magic_markdown_panel.test.mjs (existing test setup — CONFIRM whether mount() tests use a real document or need a lightweight DOM shim/JSDOM; RESEARCH Wave-0 gap flags this. If the existing panelVDom tests are pure-spec and never call mount(), add a minimal DOM shim — a tiny fake document/element with addEventListener + closest + dispatchEvent — sufficient to drive mount()'s handlers without a full JSDOM dependency)
    - backend/static/js/fe/magic_markdown_gestures.test.mjs (the resolver-level drag/double-right cases ALREADY exist here — verify the new DOM-capture layer covers the wiring without DUPLICATING the resolver assertions)
  </read_first>
  <action>
    Extend backend/static/js/fe/magic_markdown_panel.test.mjs with the six behavior cases from Task 1. Drive mount() with a minimal DOM shim (or real document if the harness already provides one — confirm first; prefer NO new external dependency, hand-roll a tiny event-dispatch shim if needed). Dispatch synthetic contextmenu / paired-contextmenu / mousedown→mousemove→mouseup sequences at fake targets and assert the correct handler (onToggle / onCollapse / onDelete / onWire) fires with the expected path/ids. Do NOT re-test resolveGesture's classification (already covered in magic_markdown_gestures.test.mjs) — test only the NEW DOM-capture-to-handler wiring. The double-right debounce test must dispatch two contextmenu events within the window and assert DELETE_REF (not two folds), and a third spaced beyond the window asserts a fold (debounce boundary).
  </action>
  <verify>
    <automated>node backend/static/js/fe/magic_markdown_panel.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node backend/static/js/fe/magic_markdown_panel.test.mjs` exits 0 with the six DOM-capture cases (no regression to existing panelVDom cases)
    - A double-right-within-window case asserts DELETE_REF; a spaced case asserts fold (debounce boundary proven)
    - No new external test dependency added (shim hand-rolled if needed)
  </acceptance_criteria>
  <done>magic_markdown_panel.test.mjs proves the contextmenu/double-right-debounce/drag DOM capture wiring against a minimal shim, with the debounce boundary asserted and no resolver-logic duplication.</done>
</task>

<task type="auto">
  <name>Task 3: e2e — hover-type-graph, drag-wire, double-right-delete, fold-preservation</name>
  <read_first>
    - frontend_e2e/object_exploration.spec.js (the spec created in Plan 03 — ADD cases to it, do not create a second file)
    - frontend_e2e/projector.spec.js (Playwright drag/mouse interaction idiom in this repo)
    - frontend_e2e/playwright.config.js (stub-boot fixture)
    - .planning/phases/WFH-07-deep-object-exploration-gestures/07-UI-SPEC.md "Fold-State Preservation Contract" + "Hover-Preview Contract"
  </read_first>
  <action>
    Add EXPLORE-03 + EXPLORE-01-hover cases to frontend_e2e/object_exploration.spec.js: (1) hover-type-graph — hover a typed field on a python-native node and assert the next-rank type graph previews (super-class + typed params rendered, sourced from Plan 01's /next_rank endpoint; the stub backend must serve a materialised python-native fixture — seed via the existing editor/materialiser path); (2) drag-wire — Playwright mouse down on a graph node, move, up on another, and assert the target gains the source's typed fields (inherit_types side-effect from Plan 04) AND the transient drag line was solid (no stroke-dasharray); (3) double-right-delete — dispatch two right-clicks within the debounce window on a wired {ref} and assert both the token AND its backing edge are gone (query the edge list); (4) fold-preservation — expand a nested fold, collapse the parent, re-expand, and assert the nested fold is restored (M.6) — this case may also drive the panel-gesture-fold-roundtrip REPL scenario as the cross-check. Reuse the stub fixture; do NOT boot real subsystems (EXPLORE-04 real run is Plan 06).
  </action>
  <verify>
    <automated>cd frontend_e2e && npx playwright test object_exploration.spec.js -g "drag-wire|double-right|hover|fold" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test object_exploration.spec.js -g "drag-wire"` passes (target inherits source typed fields; transient line solid)
    - `npx playwright test object_exploration.spec.js -g "double-right"` passes (token AND backing edge removed)
    - `npx playwright test object_exploration.spec.js -g "hover"` passes (next-rank type graph previews)
    - `npx playwright test object_exploration.spec.js -g "fold"` passes (M.6 fold preservation), AND `env-scenario --name panel-gesture-fold-roundtrip` stays green
    - `npx playwright test black_slate.spec.js` still passes (no dotted-line regression from the transient drag line)
  </acceptance_criteria>
  <done>object_exploration.spec.js proves hover-type-graph preview, drag-wire-with-inheritance, double-right-delete (token + edge), and fold-state preservation end-to-end against the stub stack; the transient drag line is solid; black_slate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM events→gesture resolver | raw mouse/keyboard events cross into gesture classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | inline gesture re-classification drifting from §18.32 | mitigate | Every handler calls the single resolveGesture(); a grep gate asserts no inline if(button===) chain (RESEARCH Don't Hand-Roll). |
| T-07-10 | Denial of Service | drag handlers leaking listeners / runaway transient render | mitigate | Drag state is torn down on every mouseup (success or discard); transient line removed; no per-frame accumulation. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase; the DOM-test shim is hand-rolled, no new dependency (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node backend/static/js/fe/magic_markdown_panel.test.mjs` exits 0 (six DOM-capture cases + debounce boundary).
- `npx playwright test object_exploration.spec.js -g "drag-wire|double-right|hover|fold"` passes.
- `env-scenario --name panel-gesture-fold-roundtrip` green (M.6); `black_slate.spec.js` green (no dasharray on the transient drag line).
- Every new handler routes through resolveGesture — no inline classification.
</verification>

<success_criteria>
- mount() captures all three missing gestures (right-click fold, double-right delete, drag-wire) plus hover next-rank preview, every one routed through the existing resolveGesture and gateway.
- The 🔒 read-only gate, M.6 fold preservation, and the solid (never dashed) transient drag line all hold, proven by unit + e2e.
</success_criteria>

<output>
Create `.planning/phases/WFH-07-deep-object-exploration-gestures/07-05-SUMMARY.md` when done
</output>
