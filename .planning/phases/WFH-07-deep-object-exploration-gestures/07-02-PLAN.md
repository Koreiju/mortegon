---
phase: 07-deep-object-exploration-gestures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/static/js/fe/magic_markdown.mjs
  - backend/static/js/fe/magic_markdown.test.mjs
autonomous: true
requirements: [EXPLORE-01]
must_haves:
  truths:
    - "Raw object inspection panels (python-native nodes) render as key:Type=value typed lines"
    - "User compute nodes stay type-stripped (no colon-type pair anywhere) — rank-1 minimalism preserved"
    - "The typed mode is gated on the same condition the legacy read-only lock uses, never a separate show-types flag"
  artifacts:
    - path: "backend/static/js/fe/magic_markdown.mjs"
      provides: "renderTypedPanel pure function emitting key:Type=value Line[] for python-native nodes"
      contains: "renderTypedPanel"
    - path: "backend/static/js/fe/magic_markdown.test.mjs"
      provides: "unit cases proving typed rendering for python-native nodes AND type-stripping for compute nodes"
      contains: "Type"
  key_links:
    - from: "magic_markdown.mjs renderTypedPanel"
      to: "node.type_hint / read_only gate"
      via: "typed mode gated on /^python_/.test(node.type_hint) || node.read_only===true (same as legacy lock)"
      pattern: "type_hint"
---

<objective>
Add a typed `key:Type=value` render mode to `magic_markdown.mjs` for raw object inspection panels (EXPLORE-01's "hovering driver: WebDriver previews the WebDriver object model"). Re-implement the legacy `cp/concept_graph.js::_pythonNativeTypedView` algorithm as a pure function over the existing `Line[]` model — NOT a literal copy (legacy operates on THREE.js DOM + Babylon-era shapes).

Purpose: `magic_markdown.mjs` today is purely structural (correct for rank-1-minimalism compute nodes) and has NO `key:Type=value` rendering path. EXPLORE-01's raw-object panels need typed lines, but N.4/N.5 compute nodes MUST stay type-stripped. The hard pitfall (RESEARCH Pitfall 3): gate typed rendering on the SAME condition as the legacy 🔒 read-only lock so types never leak onto compute-node panels. This renders the type graph that D-03's backend endpoint (Plan 01) supplies — frontend renders, never computes (D10).
Output: `renderTypedPanel` pure function + Wave-0 unit-test scaffold.
</objective>

<execution_context>
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/isaac/Documents/web_fiber_haptics/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-RESEARCH.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-PATTERNS.md
@.planning/phases/WFH-07-deep-object-exploration-gestures/07-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renderTypedPanel pure function (key:Type=value mode, gated on read-only condition)</name>
  <read_first>
    - backend/static/js/fe/magic_markdown.mjs lines 160-265 (existing renderPanel + the Line shape `{depth,text,glyph,refTarget,path,source}` + renderGraph — the model the new function must return)
    - backend/static/js/cp/concept_graph.js lines 1366-1393 (_pythonNativeTypedView — the ALGORITHM to re-implement: parse d.signature/d.ports.inputs/outputs → name: Type lines + → ReturnType for functions, or d.members list for objects)
    - backend/static/js/cp/concept_graph.js lines ~1684-1708 (the read-only 🔒 gate: /^fixture::/.test(backing_pointer) || /^python_/.test(type_hint) — the SAME condition to gate typed rendering on, per RESEARCH Pitfall 3)
    - .planning/phases/WFH-07-deep-object-exploration-gestures/07-UI-SPEC.md "Type-Slot Row Rendering Contract" (the exact `key : Type = value` row shape; compute nodes render NO colon-type pair)
  </read_first>
  <behavior>
    - Test 1: renderTypedPanel on a python-native node (type_hint starts with "python_") with a signature+ports.inputs returns Line[] where each typed row text is "key : Type = value" (or "key : Type" when no value), with a "→ ReturnType" line for the function's output.
    - Test 2: renderTypedPanel on a python-native OBJECT node (d.members list) returns one Line per member, last `::`-segment only (mirroring _pythonNativeTypedView's members map).
    - Test 3: the existing renderPanel on a user compute node (type_hint NOT python_, not read_only) is UNCHANGED — its Line[] text contains NO " : " type slot and NO "=" type pair anywhere (rank-1 minimalism preserved).
    - Test 4: the typed mode is selected by the gate condition only — a node with read_only===true but a non-python type_hint still renders typed; a node with neither stays structural.
  </behavior>
  <action>
    Add a pure exported function `renderTypedPanel(node, opts)` to backend/static/js/fe/magic_markdown.mjs that returns the project's existing Line[] shape (`{depth,text,glyph,refTarget,path,source}`). Parse the node's data JSON the way _pythonNativeTypedView does (d.signature with d.ports.inputs/outputs → typed rows; fallback to signature-string regex parse when ports.inputs absent; d.members → member list). Each typed row's text follows the UI-SPEC Type-Slot contract: "key : Type = value" (value omitted → "key : Type"; function output as a "→ ReturnType" trailing line). Skip the implicit "self" parameter (as the legacy view does). Wire selection into renderPanel: when the node satisfies the read-only gate (`/^python_/.test(node.type_hint||'') || node.read_only===true`), route through renderTypedPanel; otherwise keep the existing structural path UNCHANGED. Do NOT add a separate "show types" flag — couple typed-rendering to the read-only condition exactly as the legacy reference couples them (RESEARCH Pitfall 3 / Assumption A4). Keep renderTypedPanel a pure function (no DOM, Node-testable) so it composes with the existing panelVDom path.
  </action>
  <verify>
    <automated>node backend/static/js/fe/magic_markdown.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node backend/static/js/fe/magic_markdown.test.mjs` exits 0 with the new typed-render cases passing
    - `grep -n 'renderTypedPanel' backend/static/js/fe/magic_markdown.mjs` shows the exported function
    - `grep -nE 'type_hint|read_only' backend/static/js/fe/magic_markdown.mjs` shows the typed mode gated on the python_/read_only condition (no standalone show-types flag)
  </acceptance_criteria>
  <done>renderTypedPanel emits `key:Type=value` Line[] for python-native/read-only nodes, compute nodes stay type-stripped, the mode is gated on the read-only condition only, and the unit tests prove all four behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 unit-test scaffold for the typed-render mode</name>
  <read_first>
    - backend/static/js/fe/magic_markdown.test.mjs (existing assertion style — the `test(...)` + node:assert harness this file already uses; extend it, do not fork a new file)
    - backend/static/js/fe/integration.test.mjs lines 1-40 (the build-state → assert-Line[]-text shape to mirror)
  </read_first>
  <action>
    Extend backend/static/js/fe/magic_markdown.test.mjs with the four cases described in Task 1's `<behavior>` block (typed function row, typed object members, compute-node type-stripping invariant, gate-selection). Build a python-native fixture node (type_hint "python_function" with data containing signature+ports.inputs/outputs) and a user compute fixture node, call renderTypedPanel / renderPanel, and assert on the resulting Line[] `.text` values exactly as integration.test.mjs asserts (assert.deepStrictEqual on mapped text arrays). The compute-node invariant test is the load-bearing one: assert NO Line text matches / : .+ = / for a user compute node (rank-1 minimalism guard). These cases are the Wave-0 scaffold the rest of EXPLORE-01 verifies against — they must exist before downstream waves consume the typed mode.
  </action>
  <verify>
    <automated>node backend/static/js/fe/magic_markdown.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node backend/static/js/fe/magic_markdown.test.mjs` exits 0 (no regressions in existing cases + new typed cases green)
    - The file contains a compute-node case asserting NO " : Type = " slot leaks onto a user compute node
  </acceptance_criteria>
  <done>magic_markdown.test.mjs carries the typed-render Wave-0 cases (typed function row, typed object members, compute-node type-stripping, gate-selection), all green, with no regression to existing cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node-data→render | a node's data JSON (possibly user-authored) is parsed for typed rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | renderTypedPanel JSON parse | mitigate | Parse is rank-1 only (no recursive walk); wrap JSON.parse in a guard returning a structural fallback on malformed data, matching _pythonNativeTypedView's defensive shape. |
| T-07-04 | Information disclosure | type leak onto compute nodes | mitigate | Typed mode gated on read-only condition only; compute-node invariant test asserts no type slot leaks (rank-1 minimalism, locked design). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node backend/static/js/fe/magic_markdown.test.mjs` exits 0.
- Compute-node type-stripping invariant asserted (no `: Type =` slot on rank-1 user nodes).
- Typed mode coupled to the read-only gate, not a separate flag.
</verification>

<success_criteria>
- `renderTypedPanel` renders `key:Type=value` for python-native/read-only nodes and `→ ReturnType` for functions, while user compute nodes stay structurally type-stripped.
- The Wave-0 unit cases prove the behavior and guard rank-1 minimalism.
</success_criteria>

<output>
Create `.planning/phases/WFH-07-deep-object-exploration-gestures/07-02-SUMMARY.md` when done
</output>
