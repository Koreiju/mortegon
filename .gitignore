# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.pytest_cache/

# ── Node ────────────────────────────────────────────────────────────────
node_modules/

# ── Runtime data & artifacts (regenerated by the app/scan/REPL) ──────────
kuzu_db/
snapshots/
logs/
logs.txt
*.log

# ── Large model binaries (GPT4All / nomic GGUFs live in an external cache)
*.gguf
*.bin

# ── Heavy UI media (sidefall/waterfall backdrop — slated for removal by the §T
#    black-slate strip; regenerable, never the source of truth)
*.mp4

# ── Editor / tooling caches ──────────────────────────────────────────────
.vscode/
.lsp/
.clj-kondo/
.idea/

# ── Playwright (frontend e2e) ────────────────────────────────────────────
test-results/
test_packages/
playwright-report/
blob-report/
.playwright/
.playwright-mcp/

# ── OS ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# NOTE: .planning/ is intentionally TRACKED — GSD manages it as project memory.
# NOTE: backend/drivers/geckodriver.exe is intentionally vendored (kept tracked).
