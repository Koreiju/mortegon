# Code Architecture — Backend (split into the `backend/` suite)

> **This high-level doc has been split for clarity** into the [`backend/`](backend/) suite — one doc per service area. See [`backend/README.md`](backend/README.md) for the index and the seams.
>
> - Shared schemas → [`data_schemas.md`](data_schemas.md); wire contracts → [`contracts.md`](contracts.md).
> - No-mocks subsystem boundaries → [`subsystems.md`](subsystems.md).
> - The keep / remove / replace ledger (forbidden + legacy code) → [`migration.md`](migration.md).
